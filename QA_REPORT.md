# QA_REPORT — تقرير ضبط الجودة

> التاريخ: 2026-09-24. **إفصاح منهجية كامل**: هذه البيئة (Freebuff) تدير dev server وConvex ولا تتيح متصفحًا آليًا؛ لذلك **لم يُشغَّل التطبيق في متصفح حقيقي**. الأدوات المستخدمة فعليًا: `bun test` (اختبارات وحدة أصلية)، `bunx convex dev --once` (codegen ونشر dev)، `bunx tsc -b --noEmit` (فحص نوعي صارم)، وقراءة تدفقية للكود. أي ادعاء يتجاوز ذلك لن يُكتب هنا.

---

## 1) ما الذي نُفّذ؟ (مرتبطًا بالخطة IMPLEMENTATION_PLAN.md)

### Phase 0 — Stabilization
1. **`src/convex/planner.ts`**:
   - `removeFavorite` mutation جديد: حذف صريح عبر فهرس `by_user_and_item` — لا يُدرج شيئًا أبدًا، ويعيد `true` عند حذف فعلي.
   - `getHistory`: من 62 استعلامًا يوميًا (31 يوم × جدولين) إلى **استعلامَي نطاق** (gte/lte) لكل جدول + تجميع في الذاكرة مع احترام قائمة الأيام المطلوبة (`take(310)`).
   - `getStats`: تواريخ الثلاثين يومًا تُبنى الآن بـ `dateKey()` المحلي بدل `toISOString()` (UTC) — توحيد تعريف «اليوم» مع بقية التطبيق.
2. **`src/hooks/use-favorites.ts`**: `remove()` استبدل الاستدعاء الخاطئ `toggleFavorite({kind:"dhikr", title:"x"})` (الذي كان قد يُدرج محفوظًا وهميًا) بـ `removeFavorite({itemId})`.
3. **`src/pages/Landing.tsx` + `src/hooks/use-nudges.ts`**: حذف وصف «المصحف الورقي/بصفحات» بعد إزالة وضع الصفحات فعليًا؛ النصان الآن يصفان القارئ المستمر الحقيقي (سلس، يحفظ الموضع، ينزل كاملًا للأوفلاين). تأكيد grep: صفر إشارات متبقية في src وpublic وindex.html.

### Phase 1 — Core Value (أغلق الحلقة)
4. **`src/components/app/HomeView.tsx`**: بطاقة «ثباتك في آخر ٣٠ يومًا» (نسبة الصلوات/الأيام المتصلة الكاملة/أيام الأذكار/أضعف صلاة) من `getStats` الموجود **بلا أي backend جديد**؛ تُخفى كليًا قبل أول تسجيل (لا حالة فارغة محرجة)، و«أضعف صلاة» يُخفى إن لم يوجد. `stats` optional prop نوعه `WeekStats`.
5. **`src/pages/Dashboard.tsx`**: تمرير `stats` المُستعلم أصلاً إلى HomeView (استعلام واحد لا يُكرر).

### Phase 2 — Trust
6. **`src/pages/Landing.tsx`**: نقطة رابعة في قائمة الضمانات: «لا إعلانات، ولا يغادر موقعك جهازك إلا لخدمة المواقيت» — صياغة مطابقة للسلوك الموثق في الإعدادات.

### Phase 3 — Quality Net
7. **`tests/time.test.ts`, `tests/prayers.test.ts`, `tests/hijri.test.ts`**: 48 اختبارًا وحدة أصلية بـ `bun test` خارج `src` (لا يمس tsc ولا حزمة الويب) — تحويل دائري للدقائق، لفّ منتصف الليل، صيغ المفاتيح، أرقام هندية، قيود هجرية مترية (لا حُكميات مطلقة)، اتساق البحث الثنائي، وبنية المواقيت والاحتياطي.

## 2) الأخطاء التي وجدتها الاختبارات وأصلحتها
- **خطأ عرض حقيقي في `formatArabicTime`**: الدقائق كانت تُصاغ بأرقام لاتينية داخل وقت «عربي» (`٥:16`) — مخالفة لوعد النظام كله بأرقام هندية. أصلح بـ `toArabicDigits` على الدقائق + تعليق يوثق الكشف. (الاختباران اللذان فشلا أولًا: 05:16 صباحًا وهجريLabel — الأول كشف الخطأ والثاني كان خللًا في منطق الاختبار نفسه مع أسماء شهور من كلمتين، صُحّح فحصه إلى includes).

## 3) النتائج النهائية للأدوات
| التحقق | الأمر | النتيجة |
|---|---|---|
| اختبارات وحدة | `bun test` | **48 pass / 0 fail** (88 expect) |
| كودجين + نشر dev | `bunx convex dev --once` | Functions ready (بعد كل تغيير Convex) |
| فحص نوعي | `bunx tsc -b --noEmit` | **صفر أخطاء** |

## 4) حالات مُراجَعة بالقراءة (غير قابلة للتشغيل هنا — مُصرّح)
- **أوفلاين/جلسة**: bypass `RequireAuth` يقرأ الجلسة المخزنة فقط عند `!navigator.onLine`؛ أول استعلام Convex حقيقي سيرفض إن انتهت الجلسة — سلوك مقبول موثق.
- **إدخال غير صالح للخادم**: validators مغلقة + `assertDate/assertTime/ITEM_ID_PATTERN/cleanText` ترفضه (بقيت كما هي).
- **مضاعفة الإجراءات**: تسجيل صلاة مرتين = patch لآخر نداء (idempotent فعليًا)؛ حذف محفوظ غير موجود في الخادم = no-op آمن الآن (كان خطأ الإدراج).
- **سباقات الحفظ المحلي**: دمج المحفوظات بالاتحاد عبر `savedAt` الأعلى — بلا تغيير.

## 5) ما لم يُختبر (حدود صريحة)
- لا اختبار E2E تفاعلي، لا Lighthouse، لا قياس زمن استعلام فعلي بعد تحسين getHistory، لا فحص إشعارات على جهاز حقيقي، لا فحص تباين/قارئ شاشة شامل. هذه قائمة العمل التالية لا ادعاءات مكتملة.

## 6) Self-Critique بعد كل مرحلة
- **ما يمكن أن يكون أفضل**: اختبارات `use-favorites`/`use-reminders` تتطلب محاكاة DOM/Convex — أُجّلت عن قصد بدل إضافة مكتبات (قاعدة: لا مكتبة جديدة).
- **ما بُني مبالغًا**: لا شيء — كل إضافة ≤ 60 سطرًا وتستخدم بيانات موجودة.
- **ما قد يكسر لاحقًا**: `time.ts` يعتمد الآن على `hijri.ts` (اتجاه واحد، hijri بلا استيرادات — لا دورة؛ موثق).
- **قرار مرفوض**: تحويل `getHistory` لترقيم صفحات — الحد 310 سجلًا كافٍ لسنة كاملة يوميًا؛ الترقيم تعقيد بلا عائد الآن.
