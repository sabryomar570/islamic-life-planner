# PHASE_3_MASTER_PLAN.md

**التاريخ:** 2026-09-25
**الحالة:** `IMPLEMENTED — AUTOMATION VERIFIED — BROWSER QA PENDING`
**ما قبلها:** Phase 0 · Phase 1 · Phase 2 — كلها `AUTOMATION VERIFIED` و`BROWSER QA NOT VERIFIED`

> هذه المرحلة **تجربة وانخراط ومحتوى ديني وإشعارات وصوت**. لم تُبنَ ميزة جديدة خارج هذا النطاق،
> ولم يُمَسّ محرّك الحياة ولا منطق التخطيط والصلاة والمراجعة إلا حيث تطلّب التكامل.

---

## 1) الفلسفة الحاكمة

التطبيق رفيق حياة إسلامي هادئ. الحكم على أي تغيير في هذه المرحلة:

- **هل يهدّئ؟** لا يضيف ضوضاء ولا مؤثرا ولا تشويشا تنافسي.
- **هل يحترم؟** لا لوم ولا ضغط ولا تكرار. من كثرت التنبيهات توقّف المستخدم عنها كلها.
- **هل يفيد؟** كل شاشة تقود إلى فعل واحد مفهوم، لا إلى لوحة widgets.
- **هل يصدق؟** المحتوى الديني منسوب إلى مصدره، وما لم يُوثَّق مُعلَن أنه كذلك.

---

## 2) ما نُفِّذ

| المجال | الحالة | الملف |
|---|---|---|
| بنية بيانات الأحاديث | ✅ | `src/lib/hadith-metadata.ts` |
| بيت الأحاديث + قارئ الحديث | ✅ | `HadithView.tsx` · `HadithReader.tsx` |
| مستطيل الإحصاء المتحرك | ✅ | `InsightCarousel.tsx` · `InsightSlot.tsx` |
| رسومات تجريدية | ✅ | `Artworks.tsx` |
| نظام الصوت والتفضيلات | ✅ | `src/lib/audio.ts` · `SettingsView.tsx` |
| طبقة صياغة الإشعارات | ✅ | `src/lib/notification-templates.ts` |
| مركز الإشعارات | ✅ | `src/lib/notification-center.ts` · `NotificationCenter.tsx` |
| حالات الأزرار الموحّدة | ✅ | `Surfaces.tsx` · `src/index.css` |

---

## 3) قاعدة المحتوى الديني — غير قابلة للتفاوض

**لا نخترع حديثا ولا راويا ولا مصدرا ولا درجة.** لم يُؤلَّف نص ديني واحد في هذه المرحلة.

قاعدة البيانات الحالية فيها **ثلاثة مستويات مختلفة** من التوثيق مخلوطة في شكل واحد.
فصلها صار جزءا من العنصر المعروض:

| الحالة | المعنى | العدد اليوم |
|---|---|---|
| `verified` | كتاب + رقم حديث + مراجعة بشرية | **٠** |
| `attributed` | راوٍ ومصدر جامع معروفان، بلا رقم مثبّت | الأغلبية |
| `needs-review` | نسبة موصوفة («يُنسب») أو أثر صحابي | ١٨+ |

**قرار صريح:** لم نُصنّف شيئا كـ«موثّق». ذلك سيقفز فوق مراجعة لم تحدث.
`canClaimVerified` ترفض الوصف بلا كتاب ورقم، و`tests/hadith-metadata.test.ts` يحرس ذلك.

**يحتاج مراجعة بشرية أو مصدر خارجي:** مراجعة ١٣٣ حديثا بعلماء، وإثبات رقم كل حديث
في مصدره، وإثبات درجته. هذه ليست مهمة كود.

---

## 4) قرار الأداء: قياس قبل أي تحسين

قياس `Dashboard` بعد أول ربط أظهر ارتفاعا من **131.51 kB إلى 155.63 kB**.
السبب: `insights.ts` يستورد قاعدة الأحاديث كاملة، فانتقلت إلى المسار الحرج.
عولجت بجعل الموضع كسولا، فصارت القاعدة في chunk مستقل يُحمَّل عند الطلب.

| المقياس | Phase 2 | Phase 3 | القرار |
|---|---|---|---|
| `index` | 351.68 kB | 352.64 kB | +0.96 مقبول |
| `Dashboard` | 131.51 kB | 148.67 kB | +17.16 مقابل نظام إشعارات وصوت كامل |
| قاعدة الأحاديث | داخل المسار الحرج | `hadith` 56.54 kB منفصل | إصلاح |

الزيادة الباقية في `Dashboard` هي ثمن الميزة نفسها، وهي مقيسة لا مقدَّرة.

---

## 5) ما لم يُنفَّذ

| البند | السبب |
|---|---|
| درجات الأحاديث | لا تُخترع. تُعرض حالة التوثيق بدلا منها |
| Browser QA | لا أداة متصفح في البيئة — انظر `PHASE_3_BROWSER_QA.md` |
| طابور offline للـmutations | تغيير معماري خارج النطاق |
| موسيقى أو مؤثرات | خارج هوية التطبيق عمدا |

---

## 6) Definition of Done

- [x] بنية بيانات الأحاديث مع حالة توثيق
- [x] بيت الأحاديث وقارئ الحديث
- [x] مستطيل الإحصاء في اليوم والصلاة والمراجعة
- [x] رسومات تجريدية متناسقة
- [x] نظام الصوت والتفضيلات الخمس
- [x] طبقة صياغة الإشعارات فوق المحرّك القائم
- [x] مركز الإشعارات
- [x] حالات الأزرار الموحّدة
- [x] `bun test` 240 · `tsc` PASS · `lint` 0 errors · `build` PASS · `convex` PASS · `glyphs` نظيف
- [ ] **Browser QA: ٣٠ اختبارا** — لا متصفح في البيئة ⇒ `NOT VERIFIED`

**الحالة: `IMPLEMENTED — AUTOMATION VERIFIED — BROWSER QA PENDING`.**
ليست `RELEASE READY`، ولا يُقال «تم» قبل بوابة المتصفح.
