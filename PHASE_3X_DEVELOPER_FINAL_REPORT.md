# PHASE_3X_DEVELOPER_FINAL_REPORT.md

**التاريخ:** 2026-09-25
**النطاق:** UX / Identity فقط — قسم «المطوّر» وقسم «ادعم OUD»
**الحالة الرسمية:** `IMPLEMENTED · AUTOMATION VERIFIED · BROWSER QA NOT VERIFIED`

**ما لم يُمَس:** محرّك الحياة · منطق التخطيط والصلاة والمراجعة · المخطط والـmutations · واجهة الـAPI.
لا feature جديدة، ولا backend جديد، ولا مكتبة جديدة.

---

## 1) الملفات

### جديد

| الملف | الغرض |
|---|---|
| `src/lib/developer.ts` | هوية المطوّر · الحسابات · الدعم · المشاركة · الحافظة — منطق خالص بلا React |
| `src/lib/app-meta.ts` | الإصدار، مقروءا من `package.json` وقت البناء |
| `src/components/app/DeveloperView.tsx` | صفحة «المطوّر» |
| `src/components/app/SupportDialog.tsx` | نافذة «دعم OUD» |
| `tests/phase3x-developer.test.ts` | ٥٣ اختبارا على الشيفرة والإعدادات |
| `tests/phase3x-render.test.ts` | ١٢ اختبارا على **الرسم الفعلي** للصفحة |

### مُعدَّل

| الملف | التعديل |
|---|---|
| `src/components/app/Navigation.tsx` | `developer` في الخريطة والتسميات + مجموعة «التطبيق» |
| `src/pages/Dashboard.tsx` | تحميل كسول + فرع العرض |
| `tsconfig.app.json` | `resolveJsonModule` لقراءة الإصدار من الحزمة |

**`vite.config.ts` لم يُمسّ.** لم يكن ذلك ضروريا: الإصدار يُقرأ من `package.json` عند البناء،
و**تحققتُ من أن باقي البيان لا يصل إلى المتصفح** (`grep convex-dev dist/assets/*.js` → لا نتيجة).

---

## 2) Developer Section

الترتيب هو الحجة، وكل قسم في لوحته لا في بطاقة واحدة ضخمة:

| # | القسم | اللوح |
|---|---|---|
| ١ | **عمر صبري** — Founder & Developer of OUD + نص الفكرة | `Panel` + monogram |
| ٢ | **لماذا OUD؟** | `Editorial` — ورق دافئ، لا زجاج |
| ٣ | **تواصل معي** | `Panel` + ثلاث بطاقات + «اقتراح أو ملاحظة» |
| ٤ | **ادعم OUD** | `Panel` + زر يفتح النافذة |
| ٥ | **شارك OUD** | `Panel` |
| ٦ | **البيانات** | تذييل نصّي · الإصدار |

**الوصول:** من «المزيد» ← مجموعة **التطبيق** ← «المطوّر».
**لا وجهة جديدة في الشريط السفلي** — الشريط خمسة أقسام يستعملها المستخدم يوميّا، وصفحة عن التطبيق ليست منها. اختبار يحرس ذلك: يقطع الشريط السفلي من النص ويتأكد أن `developer` ليس فيه.

**Monogram لا صورة:** لا صورة شخصية في المشروع، ولا يجوز اختراع وجه. monogram يحمل
«OUD» — اسم المنتج لا اسم الشخص.

**بلا أرقام في «لماذا»:** لا مستخدم ولا تحميل ولا تقييم. رقم بلا مصدر دعوى، والاختبار يمنع
وجود أي رقم في هذا القسم.

---

## 3) Social configuration

**مصدر واحد، ولا تكرار في Components.** كل الروابط في `developer.ts`، والواجهة تمرّ عبر
`socialHref()` وحدها — فلا_component يعرف رابطا.

| المنصة | الرابط | ما تعرضه البطاقة |
|---|---|---|
| Telegram | `https://t.me/omarsaabry` | `Send` + «Telegram» + «محادثة مباشرة» + سهم |
| Instagram | `https://instagram.com/_rcfo` | `Instagram` + «Instagram» + «صور ومشاركات» + سهم |
| TikTok | **غير معروف** | `Music2` + «TikTok» + «مقاطع قصيرة» + «قريبًا» |

**لا اسم مستخدم في الواجهة. إطلاقا.** لا `@`، ولا معرّف مشتقّ من الرابط، ولا نصّ يقرؤه
المستخدم. الاسم الحقيقي يظهر من الرابط عند الفتح، لا من تسميتنا.

**حارس بنيوي:** رابط `javascript:` أو `data:` مرفوض بدالة `isSafeExternalUrl`، والاختبار
يمرّر الثلاثة عليها. و`socialHref` يرجع `null` لغير الآمن، فالمكوّن لا يستطيع تمرير رابط خطير
حتى لو مُرّر خطأ.

**TikTok بلا رابط مخترع.** الرابط الكامل غير معروف، فالحقل `null` والبطاقة «قريبًا» بلا
`href`. **لا `tiktok.com` في أي ملف بالشيفرة** — اختبار يمرّ على كل ملفات `src`.
الاختيار: بطاقة صادقة بلا زر، أو رابط مخترع يفتح صفحة خطأ. الأول بلا كلفة، والثاني يكلّف حسابا
حقيقيا.

**لا GitHub.** ليس في قائمة المنصات، ولا في الشيفرة، ولا في الصفحة المرسومة.

---

## 4) Support configuration + We Pay

**اللغة عن التطبيق، لا عن الشخص.** هذا هو هوية المنتج:

| | |
|---|---|
| العنوان | **ادعم OUD** |
| النص | «إذا ساعدك OUD ولو بخطوة واحدة، يمكنك دعم استمرار تطويره وتحسينه.» |
| التنبيه | «الدعم اختياري، ويساعد في استمرار تطوير التطبيق وتحسين تجربته.» |
| الزر | **ادعم OUD** |
| في النافذة | «دعم OUD» · «شكرًا لرغبتك في دعم OUD…» · «وسيلة الدعم» · **We Pay** |
| سبب التحويل | «هذه المحفظة مخصصة لاستقبال دعم تطوير تطبيق OUD.» |
| الخطوات | «يمكنك فتح We Pay وإرسال الدعم إلى رقم المحفظة أعلاه.» |

**الرقم `015 50324450` في نافذة الدعم وحدها.** لا في الصفحة، ولا في التذييل، ولا في التصدير.
اختباران يحرسان: مرسوم الصفحة لا يحوي الرقم، **والرقم مكتوب في ملف واحد فقط من شيفرة
التطبيق كلها** (الإعدادات) — النافذة تقرأ الثابت ولا تكتب رقما.

**لا زر We Pay.** لا رابط موثوق يفتح التطبيق من المتصفح، فلا نعد بغير ما نقدر. المحلول:
رقم + نسخ + سطر الخطوات. اختبار يمنع أي `wepay` في الشيفرة.

**لا معالجة دفع داخل OUD.** لا مفتاح، ولا بوابة، ولا استدعاء شبكة. الرقم `public display
information` بطلبه صريح.

**الاقتراح أو الملاحظة** يذهب إلى Telegram — قناة اتصال حقيقية موجودة. **ولا نظام تذاكر
جديد ولا جدول ملاحظات في القاعدة.**

---

## 5) Copy behavior

ثلاث حالات لا حالتين:

| الحالة | النتيجة |
|---|---|
| `copied` | «تم نسخ رقم المحفظة» + علامة صح داخل الزر |
| `failed` | «تعذّر النسخ تلقائيًا. الرقم ظاهر، انسخه يدويًا» |
| `unavailable` | نفس رسالة الفشل — ولا نُخطئ المستخدم وهو ليس مخطئًا |

**الرقم يبقى ظاهرًا في الحالات الثلاث.** فشل النسخ يعني أن الحافظة مغلقة، لا أن الرقم اختفى.
فلا يخرج المستخدم من النافذة بلا طريقة. اختبار يحرس أن الشرط لا يغلق النافذة.

**ما يُنسخ:** `01550324450` بلا مسافات — لحقل المحفظة في We Pay.
**ما يُعرض:** `015 50324450` — للعين. الاختبار يتحقق أن الرقمين نفس الأرقام.

**لا اعتماد على toast:** الرسالة `role="status"` داخل النافذة، فلا تضيع إن تعطّل الإشعار.

---

## 6) Share

`shareOud()` في `developer.ts` — منطق قابل للاختبار، والمكوّن يعرض الأثر فقط:

| الحالة | النتيجة |
|---|---|
| `navigator.share` موجودة | تُفتح ورقة المشاركة → `shared` |
| غير موجودة | يُنسخ الرابط → `copied` |
| المستخدم ألغى أو المتصفح رفض | يُنسخ الرابط → `copied` |
| ولا مشاركة ولا حافظة | `failed` — ولا يقول «نجح» |

**لا تُخرج المستخدم بنقرة لم تفعل شيئا.** لذلك الإلغاء يعود إلى النسخ لا إلى لا شيء.

النص: «OUD — نظام حياة يساعدك على تنظيم يومك حول صلاتك وعبادتك.»

---

## 7) Accessibility

| البند | التنفيذ |
|---|---|
| هدف لمس | كل زر من `QuietButton` / `PrimaryButton` — `touch-target` ٤٤×٤ من نظام التصميم |
| دلالات | الروابط `<a href>`، والأزرار `button` عبر النظام — **بلا `<button>` مكتوب يدويا** |
| تسميات | `aria-label` يصف الفعل: «Telegram — محادثة مباشرة. يفتح في تطبيق جديد» |
| روابط خارجية | `target="_blank" rel="noopener noreferrer"` |
| تركيز | `focus-ring` على البطاقات + حلقة `:focus-visible` واحدة من Phase 2 |
| النافذة | Radix Dialog: `DialogTitle` + `DialogDescription` + حبس التركيز + Escape + استعادة التركيز |
| إعلان النسخ | `aria-live="polite"` + نقل التركيز إلى الرقم بعد النجاح |
| أيقونات | كل أيقونة `aria-hidden` — والاختبار يفحص كل عنصر يحمل `size-*` |
| نجاح غير لوني | `ActionGlyph` + `action-success` من نظام الحالات |

---

## 8) Responsive + RTL

**مبادئ مطبَّقة في الشيفرة، لا مُدَّعاة:**

- **بلا تجاوز أفقي:** كل نص داخل `min-w-0 flex-1` فيتّقص بدل أن يدفع. الأيقونة `shrink-0`.
- **بلا لوحة ضخمة:** ستة أقسام بتباعد `stack` (١٫٥rem)، لا حاوية واحدة.
- **الأيقونة لا تنكمش:** `size-10` و`size-14` ثابتان عبر المقاسات.
- **الرقم `dir="ltr"`** مع `font-mono` وتباعد: أرقام المحفظة لا تنعكس في سياق RTL.
- **السهم `ArrowUpLeft`:** في RTL يخرج نحو اليسار، فيتحرك مع اتجاه القراءة.
- **خصائص منطقية:** `start` / `ms-auto` / `flex` — صفر `ml-` / `pr-` / `text-left` في الملفات الجديدة.

**الحدّ الصريح:** المقاسات الستة (٣٦٠ · ٣٩٠ · ٤١٢ · ٧٦٨ · ١٠٢٤+) **لم تُختبر**. لا متصفح في
البيئة. ما سلوك البناء آلي، أمّا التخطيط فلا.

---

## 9) Micro-interactions

| التفاعل | التنفيذ |
|---|---|
| دخول الصفحة | `motion-swap` على اللوح الأول — `stage-enter` هادئ |
| ضغط البطاقة | `motion-press` + `hover:bg-white/80` |
| نجاح النسخ | `ActionGlyph` + `action-success` — حلقة وخط داخلي، لا درجة لون |
| النافذة | انتقال Radix القياسي |
| الأزرار | `default` · `hover` · `pressed` · `active` · `disabled` · `loading` · `success` |

**بلا حركة دائمة، وبلا مكتبة جديدة.** لا `framer-motion` في الملفات الجديدة — والاختبار يمنع
عودته. و`prefers-reduced-motion` يلغي `motion-press` وحلقة التحميل من Phase 3.

---

## 10) Security

| | |
|---|---|
| مفاتيح | **صفر.** لا API key ولا service role ولا private key |
| أسرار | **صفر.** لا شيء في `.env` ولا في الشيفرة |
| معالجة دفع | **لا وجود لها.** لا بوابة، لا استدعاء شبكة، لا تخزين |
| رقم المحفظة | بيانات عامة معروضة، بقصد صريح من الطلب |
| روابط | `https` فقط — `javascript:` و`data:` مرفوضان بدالة |
| النافذة الخارجية | `noopener noreferrer` |

---

## 11) Tests

**٦٦ اختبارا جديدا** في هذه المرحلة (٢٧٨ ← ٣٤٤).

| الملف | العدد | ما يحرسه |
|---|---|---|
| `phase3x-developer.test.ts` | ٥٣ | الوصول في الخريطة · الهوية · الروابط الثلاثة · لا اسم مستخدم · لا GitHub · الدعم · الرقم · النسخ · اللغة · المشاركة · الوصولية · الإصدار |
| `phase3x-render.test.ts` | ١٢ | **الرسم الفعلي** للصفحة: النص المرئي، الترتيب، غياب الرقم، الروابط، غياب GitHub |

**أقوى اختبار في المجموعة هو اختبار الرسم.** الشيفرة تكذب بسهولة: يكفي أن يكتب المبرمج `hadith.source` في الترتيب فيمرّ فحصُ القراءة. أما الرسم فيعطي ما سيراه المتصفح بالضبط.
منه نثبت أن **المعرّف لا يظهر نصّا** (ويظهر داخل `href` فقط)، وأن **الرقم غائب**، و**أن
الروابط الخارجية اثنان لا ثلاثة**.

**الاختبارات الثلاثة التي لم أضعها لتُمرّر بلا فائدة:**

1. *«الوعد ليس خدمة»* — كُتب أول مرة على الملف كله، فسقط بسبب كلمة «اشتراك» في **تعليق**
   لا في نص معروض. صحّحتُ الاختبار ليتحصّ النصوص المعروضة فقط.
2. *«الرقم في ملفين»* — توقعتُ `[الإعدادات، النافذة]`، والقياس أعطى **ملفا واحدا**. وهو
   أفضل مما توقعت: الرقم مكتوب مرة واحدة في التطبيق كله. صحّحتُ التوقع إلى الواقع.
3. *«الأيقونات مخفية»* — التعبير النمطي فحص `className` فقط فلم يرَ `aria-hidden` بعدها.
   صحّحتُ الحساب لا المصدر… ثم أضفت `aria-hidden` للأيقونتين احتياطا.

**الاختبارات الثلاثة الأولى لم تكن تنويها، وجدت أخطاءها.**

---

## 12) Quality Gates

```text
bun test              344 pass / 0 fail / 3732 expect() calls / 26 files
bun tsc -b --noEmit   PASS
bun run lint          0 errors / 26 warnings
bun run build         PASS
bun convex dev --once PASS — Convex functions ready
bun run check:glyphs  نظيف
```

**كل البوابات خضراء.** تحققت من كل واحدة على حدة، وأصلحت ما أوقفها:

| ما أوقف البوابة | السبب | الإصلاح |
|---|---|---|
| `lint` × ١ | متغيّر `ok` معرّف ولا مستعمل في اختبار النسخ | حذفه + تعليق يشرح الاختبار |
| `check:glyphs` × ٢ | محرف صيني تسرّب إلى تعليقَي اختبار | إعادة الصياغة بالعربية |
| `check:glyphs` × ١ | محرفان صينيان في تعليق اختبار ثالث | إعادة الصياغة بالعربية |

**الحجم بعد البناء:**

| الحزمة | قبل | بعد | القرار |
|---|---|---|---|
| `index` | 352.64 kB | **352.64 kB** | لم يمسّها شيء |
| `Dashboard` | 148.94 kB | **149.89 kB** | ‎+٠٫٩٥ kB — استيراد كسول + فرع عرض |
| `DeveloperView` | — | **10.52 kB** (3.79 gzip) | كسولة، خارج المسار الحرج |

**لم يدخل `index` بايت واحد** من هذه المرحلة. الصفحة تُحمَّل عند فتحها فقط.

---

## 13) Browser QA

**NOT VERIFIED. لا أداة متصفح في البيئة.**

**الدليل:** لا `playwright` ولا `puppeteer` ولا `selenium` ولا `webdriver` في `node_modules`
(الاسم المطابق الوحيد `electron-to-chromium` وهو قاعدة بيانات)، ولا `chromium` ولا `chrome`
ولا `firefox` على `PATH`، ولا `~/.cache/ms-playwright`.

| الاختبار المطلوب | الحالة |
|---|---|
|Developer section | NOT VERIFIED — **لكن الرسم الآلي يثبت النص والترتيب** |
| RTL | NOT VERIFIED — الخصائص منطقية، والعين لم ترَ |
| Light / Dark | NOT VERIFIED |
| ٣٦٠ · ٣٩٠ · ٤١٢ · ٧٦٨ · ١٠٢٤ | NOT VERIFIED |
| الروابط | NOT VERIFIED — **لكن العناوين ثابتة ومختبَرة آليا** |
| لا usernames ظاهرة | **آليا PASS** — لا تظهر في النص المرئي ولا في الصفحة |
| نافذة الدعم | NOT VERIFIED — **الرسم الآلي يعطي صفرا**: Radix portal بلا DOM |
| رقم We Pay | آليا: **صحيح وغائب عن الصفحة** |
| النسخ | آليا: **النجاح والفشل والغياب** — لا النقر على جهاز |
| فشل الحافظة | آليا: **مُحاكى برفض** — لا في متصفح حقيقي |
| تركيز لوحة المفاتيح | NOT VERIFIED |
| reduced motion | NOT VERIFIED |
| إغلاق النافذة | NOT VERIFIED |
| بلا تجاوز أفقي | NOT VERIFIED |

**الحدّ الذي يعلنه هذا التقرير:** «نافذة الدعم تُرسم على الشاشة» و«التخطيط عند ٣٦٠ بكسل»
و«التركيز ينتقل ويُستعاد» — **ثلاثة أحكام لم أحكم فيها، لأن المتصفح غائب.** اختبار الرسم
يسجّل هذا الحدّ صراحةً في.test بدل أن يتركه ضمنيا.

---

## 14) Known limitations

1. **الرسم بلا DOM يساوي صفرا لنافذة الدعم.** Radix ينقل المحتوى إلى `portal`. محتوى النافذة
   محروس على مستوى الشيفرة (الرقم · السبب · النسخ · الفشل · بقاء الرقم ظاهرا)،
   **ولا assertion على ظهوره فعلا.** هذا اختبار حدود، لا اختبار نجاح.
2. **بطاقة TikTok «قريبًا»** ستبقى كذلك حتى يُضاف الرابط الكامل في
   `DEVELOPER_SOCIAL_LINKS.tiktok`. تعديل واحد، والاختبار يلين.
3. **لا زر «فتح We Pay».** لا رابط موثوق. **هذا نقص مقصود** لا يُغلق بإضافة تخمين.
4. **حجم `package.json` يُقرأ في وقت البناء.** `resolveJsonModule` في `tsconfig.app.json`.
   تحققت أن باقي البيان **لا يصل** إلى المتصفح. ولو تغيّرت أداة البناء، أُعيد الفحص.
5. **٣٠ اختبارا بصريا لم تُنفَّذ** (وهي نفسها المعلّقة من Phase 0 و 1 و 2 و 3).
6. **«اقتراح أو ملاحظة» إلى Telegram** لا صندوق وارد ولا نظام تذاكر. قرار مقصود ضمن
   «لا backend جديد».
7. **لا أرقام عن المشروع** في «لماذا OUD؟» — لا مستخدمين ولا تحميلات. الأرقام تحتاج مصدرا.

---

## 15) Release readiness

| المستوى | الحالة |
|---|---|
| **PHASE 3.x — IMPLEMENTED** | ✅ |
| **AUTOMATION VERIFIED** | ✅ ٣٤٤ اختبارا · tsc · lint · build · convex · glyphs |
| **BROWSER VERIFIED** | ❌ **NOT VERIFIED** |
| **RELEASE READY** | ❌ **لا** |

> **السبب الوحيد الذي يمنع RELEASE READY هو بوابة المتصفح.** كل ما عداه أخضر.
> وهو أخطر بند في هذه المرحلة: **كل ما سبق آلي، والشيء الوحيد الذي لم يُختبر هو ما يراه
> المستخدم بعينه.**

> **لم تُبدأ Phase 4.**
