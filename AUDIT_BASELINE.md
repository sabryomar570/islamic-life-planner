# AUDIT_BASELINE — خط الأساس قبل أي تغيير

> تاريخ التدقيق: 2026-09-24 • المنتج: «عود» — PWA إسلامي (Vite + React + TS + Convex + Tailwind + shadcn/ui، Bun)
> منهجية: قراءة فعلية للكود وتتبع الـ flows. كل دليل أدناه مأخوذ من ملف ومسار محدد. **لم يُشغَّل المشروع في متصفح حقيقي خلال هذا التدقيق** — ما يمكن التحقق منه فعليًا هو codegen + typecheck، وسنصرّح بذلك في كل تقرير.
> ملاحظة بيئة: Freebuff يدير dev server وConvex؛ لا يمكن تشغيل متصفح آلي هنا. الاختبارات الواقعية المتاحة: `bunx convex dev --once` (codegen/نشر dev) و`bunx tsc -b --noEmit`.

---

## 1) ما الذي يعمل؟ (مؤكّد بالكود)

- **Auth**: Convex Auth بمزودين Email-OTP + Anonymous (`src/convex/auth/emailOtp.ts`، `AuthConfig` في `src/convex/auth.ts`). OTP مكون من 6 أرقام عشوائية عبر `crypto.getRandomValues`، عمره 15 دقيقة.
- **حماية المسارات**: `/dashboard` داخل `RequireAuth` (تحقق جلسة + bypass أوفلاين مدروس عبر `hasStoredSession()` عند `!navigator.onLine` — `src/components/app/RequireAuth.tsx`، `src/lib/auth-storage.ts`).
- **Backend**: كل الـ mutations في `src/convex/planner.ts` تستدعي `requireUserId`؛ حقن قيم غير شرعية مستحيل عمليًا بفضل validators مغلقة (`v.union` + literals) وأنماط `DAY_PATTERN/TIME_PATTERN/ITEM_ID_PATTERN` وتنظيف نصوص من أحرف تحكم.
- **Schema**: جداول `profiles/prayerLogs/adhkarLogs/favorites` مع فهارس مركّبة صحيحة (`src/convex/schema.ts`).
- **حلقة القيمة اليومية**: تسجيل صلاة (4 حالات) → أذكار (5 أنواع) → إحصاءات 30 يومًا (نسب، streak، أفضل أسبوع، أضعف صلاة) — `planner.ts` + `StatsTable`.
- **المصحف أوفلاين فعليًا**: سلسلة تخزين ذاكرة → IndexedDB → نسخة مدمجة (`src/data/quran-offline.ts`) → شبكة بمصدرين، مع فصل البسملة حرفيًا وتخطي الفاتحة والتوبة (`src/lib/quran-store.ts`).
- **PWA حقيقية**: `public/sw.js` (shell cache-first، صفحات network-first، مضيفات API مصنفة، رسائل CACHE_URLS/NOTIFY/SKIP_WAITING)، manifest بعناصر كاملة وshortcuts.
- **التذكيرات**: مولّد جدول (صلوات/وارد/نوم/جمعة/رمضان) + نافذة إطلاق 10 دقائق + إزالة تكرار يومية (`src/hooks/use-reminders.ts`).
- **الأداء العام**: manualChunks مدروس في `vite.config.ts`، مؤقّتات مشتركة bucketed في `use-clock.ts`، بحث محلي في كل الأقسام النصية.

## 2) ما الذي لا يعمل؟

- **إزالة من «محفوظاتي» كتب خادمًا خطأً**: `use-favorites.ts` — `remove()` يستدعي `toggleFavorite({kind:"dhikr", title:"x"})`. إذا كان العنصر غير موجود على الخادم (مثلًا حُفظ محليًا وأُفلتت مزامنته) فالـ mutation **سيدرج** محفوظًا وهميًا بعنوان "x" بدل حذفه. خطأ منطقي مؤكد بالقراءة.
- **استعلام تاريخ N-query**: `getHistory` ينفّذ حتى `31×2 = 62` استعلامًا مفهرسًا لكل نداء بينما `getStats` في نفس الملف يستخدم نطاق `gte` واحد لكل جدول — تضخيم زمن الاستجابة وفواتير القراءة.

## 3) ما الذي يعمل ولكنه سيئ؟

- **نصوص قديمة تتحدث عن ميزة حُذفت**: وصف المصحف «بصفحات كالمصحف الورقي» ما زال في `src/pages/Landing.tsx` (قسم SECTIONS) و`src/hooks/use-nudges.ts` (نص نudge قسم quran) رغم حذف وضع المصحف (`lib/mushaf.ts` وMushafPage). وعدٌ للواجهة لا يُنفَّذ = كسر ثقة.
- **`getStats` يقيس من منتصف ليل UTC**: يبني `dates` من `toISOString().slice(0,10)` (`planner.ts`) بينما بقية التطبيق يستخدم `dateKey()` بالتوقيت المحلي. لمستخدمي UTC+3 مثلًا يظهر «اليوم» بشريحة مختلفة قرب منتصف الليل؛ الرقم صحيح محليًا في معظم اليوم لكن التعريف غير متسق مع بقية التطبيق.
- **تنبيه «wird» نصه يلمّح لصفحات المصحف** («صفحة واحدة بحضور قلب») — مقبول كصياغة روحية لكنه جزء من نفس الانزياح النصي.
- **`remove` يعتمد على معرفة العميل بـ kind** — لا يوجد مسار حذف صريح من الخادم (انظر 2).

## 4) ما الذي ينقص المشروع؟

- **صفر اختبارات آلية** (وحدة أو تكامل). الاعتماد كليًا على typecheck.
- **لا ملف أمان/سياسة خصوصية** للمستخدم النهائي (الإعدادات تشرح المصادر، وهذا جيد لكنه ليس سياسة).
- **لا rate-limiting** على إرسال OTP (المزود المنصوي يتحكم بذلك جزئيًا، غير قابل للتحقق هنا).
- **لا i18n** — عربي فقط (قرار منتج مشروع، لكن يُحسب في التوسع).
- **لا أدوات مراقبة/أخطاء** (Sentry أو ما يشبهها).
- **جلسة زائر الأوفلاين**: bypass `RequireAuth` عند عدم الاتصال يقرأ الجلسة المخزنة دون تحقق — قبول معقول لمطابقة سلوك PWA، لكنه يستحق توثيقًا كمخاطرة منخفضة.

## 5) أكبر المخاطر التقنية

1. **سر مضمّن في ملف منصوي**: `src/convex/auth/emailOtp.ts` يحمل `x-api-key: "fb_email_…"` حرفيًا. الملف من إنشاء القالب (platform-provided) والقيمة تُرسل إلى واجهة منصّة Freebuff نفسها — ليس سرًا يشغّل خدمة خارجية من طرفنا، لكن بقاءه في git يخالف قاعدة «لا أسرار في الكود». القرار: **لا نلمسه** لعدم كسر تدفّق OTP المنصوي (تنبيه في التقرير فقط، مع توصية استبدال مستقبلية عبر env).
2. **اعتماد خارجي للمواقيت** (Aladhan) مع تخفيف جيد: cache 6h + آخر قيمة محفوظة + FALLBACK_TIMINGS — مخاطرة منخفضة بالفعل.
3. **cap المحفوظات 500** بدون ترقيم صفحات: مقبول للأفراد، سيئ كقصة نمو.
4. **`schemaValidation: false`** في `schema.ts`: يسمح بوثائق قديمة الشكل؛ الـ validators عند الحدود تعوّض معظمه.

## 6) أكبر مشاكل UX

1. **وعد غير منفّذ في نصوص واجهة** (المصحف «بصفحات») — يُقرأ في صفحة الهبوط وأول انطباع.
2. **حذف محفوظ يتصرف بشكل غامض** (يعيد إدراج عنصر "x") — قد يظهر للمستخدم عنصر غير مفهوم في قائمته.
3. **نافذة «هل صلّيت؟» بعد الصلاة** تجيد الهدف لكن لا تمنع النقر المزدوج (زرّان، بلا قفل pending) — احتمال تسجيلين متضاربين نظريًا (الخادم patch لآخر نداء، فالضرر محدود لكن الملاحظة صحيحة).
4. **Onboarding جيد** (7 أسئلة + حفظ + إذن إشعارات) لكن لا توجد خطوة «جرّب بنفسك» بعد الحفظ — تدخل مباشر إلى Dashboard مزدحم.

## 7) أكبر مشاكل المنتج

- **لا حلقة استبقاء خارج التنبيهات**: لا أهداف أسبوعية ولا تقرير أسبوعي للمستخدم.
- **المحتوى غني لكن الاكتشاف داخله يعتمد على فلتر + بحث نصي بسيط** — كافٍ للحجم الحالي.
- **لا هوية بصرية داكنة كاملة للـ PWA على iOS** (theme-color ثابت نهاري في index.html).

## 8) أكبر مشاكل الأمان

| المشكلة | الدليل | الشدة |
|---|---|---|
| API key مضمّن في المصدر | `src/convex/auth/emailOtp.ts` | متوسطة (سر منصوي، يُنصح بنقله لـ env لاحقًا) |
| لا تحقق ملكية في `toggleFavorite` إضافي غير `userId` | موجود فعليًا في الفهارس (`by_user_and_item`) — مقبول | منخفضة |
| bypass جلسة أوفلاين بلا تحقق توقيع | `RequireAuth.tsx` | منخفضة (الرمز لا يُستعمل قبل عودة الاتصال؛ Convex نفسه سيرفض عند أول استعلام) |
| CSP غير مُعرّف في index.html | يُدار من المنصّة | منخفضة-متوسطة |

## 9) أكبر مشاكل الأداء

- `getHistory` N-queries (مذكور أعلاه) — الإصلاح سهل ومباشر.
- `quran-offline.ts` مدمج في الحزمة (حجم مصحف كامل) — مقصود لهدف «أوفلاين كامل»، وmanualChunks يعزل الضرر. مقبول.
- لا توجد قياسات فعلية (لا Lighthouse هنا) — لا ندّعي أرقامًا.

## 10) الأشياء التي يجب عدم لمسها (جيدة كما هي)

- سلسلة تخزين المصحف و`splitBasmala` — منطق دقيق ومُختبر منطقيًا بالقراءة.
- نظام التفضيلات `sakinah:prefs:v1` مع تحقق عند القراءة.
- `use-clock.ts` (مؤقّتات مشتركة) — pattern ممتاز.
- صارم `cleanText/assertDate/assertTime` في الخادم.
- بنية ManualChunks وتقسيم vendor.
- أسئلة البداية السبعة وربطها بالواجهة (كل إجابة تُغيّر شيئًا فعليًا).

## 11) ما يجب إعادة بنائه بالكامل

لا شيء يستحق إعادة بناء كاملة. الأقرب: `use-favorites.remove` + مسار حذف خادمي صريح (إصلاح مركّب صغير، ليس إعادة بناء).

## 12) التقييم المحوري (0–100) — كل رقم مرتبط بدليل

| المحور | الدرجة | أدلة محورية | أكبر فجوة |
|---|---|---|---|
| Product | 68 | قيمة يومية حقيقية (صلوات/أذكار/مصحف أوفلاين)؛ لا حلقة استبقاء أسبوعية | تقرير أسبوعي/أهداف |
| UX | 70 | Onboarding موجّه، تحويل مركزي بالـ payload؛ نصوص واجهة تعد بميزة محذوفة | اتساق الوعود مع الواقع |
| UI | 82 | نظام Glass موحّد (GlassCard/GlassPill)، خطوط عربية مضبوطة، داكن مُصلح | لمسات دقيقة في الحالات الفارغة |
| Functionality | 78 | كل قسم يعمل منطقيًا مع بيانات حقيقية مدمجة | خطأ remove، صفر اختبارات |
| Architecture | 80 | فصل واضح lib/hooks/components/convex، validators مركزية | N-query getHistory |
| Security | 62 | كل mutation محمي، مدخلات مُنقّاة؛ سر مضمّن، لا CSP صريح، لا rate-limit خاص بنا | إدارة الأسرار |
| Performance | 74 | chunks + مؤقّتات مشتركة + cache مواقيت؛ N-query يستنزف دون داعٍ | getHistory |
| Reliability | 76 | fallback مواقيت، أوفلاين مصحف، دمج محفوظات محلي/خادم | لا مراقبة أخطاء |
| Scalability | 71 | فهارس صحيحة، caps واعية (500/31/1000) | ترقيم صفحات المحفوظات لاحقًا |
| Accessibility | 60 | aria-labels مقتضبة في مواضع، تباين جيد؛ لا فحص شامل | مراجعة keyboard/contrast شاملة |
| Code Quality | 79 | تعليقات عربية ذات معنى، تسمية واضحة، لا TODO عالقة | صفر اختبارات |
| Competitive Position | 63 | أوفلاين كامل + خصوصية محلية (نقطة قوة نادرة)؛ يغيب ما يصنع الولاء اليومي | البُعد التجميعي/التقارير |

**قراءة المجموع**: لا نعلن «معدلًا سحريًا». الشكل العام: منتج قابل للاستخدام فعليًا اليوم، بجودة هندسية أعلى من المتوسط لهذا الحجم، يعيقه خطآن وظيفيان حقيقيان (remove، النصوص)، وضغط صفر-اختبارات، وبُعد ولاء منتجي غائب.

---

### قائمة إصلاحات Phase 0 المرشّحة من هذا الأساس (تُنفَّذ فورًا بعد الموافقة الذاتية بالأدلة أعلاه)

1. `use-favorites.remove` → مسار حذف خادمي صريح `removeFavorite(itemId)` (لا إدراج بأي حال).
2. إصلاح النص القديم في `Landing.tsx` و`use-nudges.ts` (وصف المصحف بدون «صفحات»).
3. `getHistory` → استعلام نطاق واحد لكل جدول (نفس أسلوب `getStats`).
4. توحيد `getStats` على `dateKey()` المحلي ليتسق مع بقية التطبيق.
5. توثيق قرار عدم لمس `emailOtp.ts` في التقرير النهائي مع توصية env.
