# PHASE_3_FINAL_REPORT.md

**التاريخ:** 2026-09-25
**الحالة الرسمية:** `IMPLEMENTED — AUTOMATION VERIFIED — BROWSER QA NOT VERIFIED`
**ما قبلها:** Phase 0 · Phase 1 · Phase 2 — كلها `AUTOMATION VERIFIED` و`BROWSER QA NOT VERIFIED`

---

## 0) تصحيح أرقام هذا التقرير

التقرير الأول كُتب قبل أن يُقاس كل شيء، فكانت أرقامه تقديرية. **هذه الجولة صحّحتها بالأداة لا بالتقدير**،
والتغييرات جوهرية في ثلاثة مواضع:

| الموضع | كان مكتوبًا | الصحيح بالمقياس |
|---|---|---|
| عدد النصوص | ١٣٣ | **١٣٠** |
| «منسوب بصيغة موصوفة» | ١٨ | **٤** |
| مصادر «آثار الصحابة» | ١٢ | **١٥** |
| حالات المراجعة | `attributed` · `needs-review` | **`traceable` · `unverified`** |

**لماذا انخفض عدد المنسوب من ١٨ إلى ٤؟** لأن ١٤ منها مصادرها «من آثار الصحابة»، والمصنِّف يفحص
المصدر قبل الراوي. راويها منسوب، لكن كلامها كلام الصحابة نفسها لا كلام النبي ﷺ. فالأثر أصدق
من النسبة، و٤ هو الصحيح. **الرقم الأول كان يخلط المنسوب بالأثر فيعطي المنسوب أصغر وحمله أكبر.**

---

## 1) What changed

المرحلة الثالثة **تجربة وانخراطا ومحتوى دينا وإشعارات وصوتا**. لم تُبنَ ميزة جديدة خارج النطاق،
ولم يُمسّ محرّك الحياة ولا منطق التخطيط والصلاة والمراجعة.

أهم ما خرج من التدقيق: **قاعدة الأحاديث ليست على درجة واحدة من التوثيق**. كانت تعرض أثرا من كلام
الصحابة بمظهر حديث مرفوع. صار ذلك معروضا لا مخفيّا، ومقيسا لا مقدَّرا.

---

## 2) Files changed

### جديد — منطق خالص (بلا React وبلا Convex)
- `src/lib/hadith-metadata.ts` — الأنواع وحالة التوثيق وسطر الاستشهاد والموضوعات
- `src/lib/audio.ts` — نظام الصوت والتفضيلات والنغمات
- `src/lib/notification-templates.ts` — طبقة الصياغة والتوقيت والتهدئة
- `src/lib/notification-center.ts` — سجل الإشعارات وحالة القراءة
- `src/lib/insights.ts` — محتوى مستطيل الإحصاء

### جديد — مكوّنات
- `src/components/app/Artworks.tsx` — خمسة رسوم تجريدية
- `src/components/app/InsightCarousel.tsx` — المستطيل المتحرك
- `src/components/app/InsightSlot.tsx` — غلاف كسول للمستطيل
- `src/components/app/HadithReader.tsx` — قارئ الحديث
- `src/components/app/NotificationCenter.tsx` — المركز والجرس
- `src/hooks/use-notification-center.ts` — محرّك التقييم

### جديد — اختبارات
`tests/hadith-metadata.test.ts` (٢٦) · `tests/audio.test.ts` (١٥) · `tests/notification-hooks.test.ts` (٣٩)
· `tests/phase3-ux.test.ts` (١٧) · `tests/date-boundary.test.ts` (١٧) · **`tests/phase3-hardening.test.ts` (١٥) · جديد**

### مُعدَّل
- `src/data/hadith.ts` — حقول `book` و`hadithNumber` و`grade` اختيارية
- `src/lib/notification-intelligence.ts` — فئة `hadith` بأولوية دنيا
- `src/lib/local-data.ts` — مفتاح الإشعارات + **عهد الجهاز** (٠ → ٧)
- `src/hooks/use-preferences.ts` — مفاتيح الصوت + `audioPreferencesOf`
- `src/components/app/Surfaces.tsx` — `ActionState` وحالات التحميل والنجاح
- `src/index.css` — `action-active` · `action-success` · `action-disabled` · `action-spinner`
- `src/components/app/HadithView.tsx` — بيت أحاديث + سطر شرح النسبة
- `src/components/app/AppHeader.tsx` — فتحة للجرس
- `src/components/app/{HomeView,PrayerView,DailyReview}.tsx` — المستطيل الكسول
- `src/components/app/SettingsView.tsx` — قسم الصوت الكامل
- `src/pages/Dashboard.tsx` — المركز، فتح الصوت، لقطة الحالة
- `scripts/schema-probe.mjs` — **أُعيد بناؤه** (كان معطوبا)
- `package.json` — بوابة `check:schema`

---

## 3) Hadith architecture

**لم يخترع شيء.** البنية جاهزة لمصدر موثوق، وغير مملوءة بتخمينات.

```text
Hadith { id, text, narrator, source, section, action,
         book?, hadithNumber?, grade? }   ← الثلاثة الأخيرة فارغة اليوم
```

### التوزيع الحقيقي بالمقياس

| النوع | العدد | الشرط | النبرة |
|---|---|---|---|
| **مرفوع** | ١١١ | راوٍ محدّد + مصدر كتابي، بلا نسبة موصوفة | `raised` — نقطة مصمتة |
| **أثر صحابي** | ١٥ | المصدر فيه «من آثار/كلام الصحابة» | `companion` — حلقة مفرّغة |
| **منسوب** | ٤ | الراوي منسوب بصيغة موصوفة، ومصدره ليس أثرا | `uncertain` |
| **ينقصه توثيق** | ٠ | راوٍ أو مصدر ناقص | `uncertain` |
| **المجموع** | **١٣٠** | | |

**صفر حديث موثّق.** وهذا صحيح لا فخر: لا رقم في مصدر، ولا مراجعة بشرية، فلا ادّعاء.

### الفصل البصري لا اللفظي

ثلاث نغمات بصرية لا أربع، لأن المشغول لا يقرأ أربع شارات. والأثر **لا يأخذ شكل المرفوع أبدا**:
نقطة مصمتة للمرفوع، وحلقة مفرّغة لغيره. شكلان مختلفان فلا يخطئ العين سريعا، ثم يأتي النص صريحا
لئلا يخطئ القارئ البطيء. اختُبرت القاعدة: `HADITH_KIND_TONES` لا تعطي `raised` لغير المرفوع.

**المواضيع:** ٢٦ بابا ← ١١ موضوعا. الأبواب باقية، والموضوع طبقة تصفية فوقها.

---

## 4) أمانة المحتوى — ما الذي يعرض للمستخدم

| السطح | ما يعرضه |
|---|---|
| شارة النسبة | اسم النوع فقط: «حديث مرفوع» · «أثر صحابي» · «منسوب» · «ينقصه توثيق» |
| في القارئ | النسبة + شرحها + حالة المراجعة العلمية |
| في بطاقة حديث اليوم | الشارة + **سطر شرح** ما يعنيه ذلك |
| نص النسخ والمشاركة | يبدأ بالنسبة نفسها، فلا يخرج الأثر مصحوبا بلا وسم |
| اقتباس المستطيل | مصدره يبدأ بالنسبة، ثم الراوي، ثم المصدر |

**لا تسمية توهم بتوثيق أعلى من الحقيقة.** اختبار يمنع وجود «موثّق» أو «صحيح» أو «متواتر»
في اسم أي نوع، واسم الحالة غير الموثّقة لا يحمل صيغة «موثّق برقمه».

**الدرجة لا تظهر إلا إذا سُجّلت، وسجل الدرجات فارغ اليوم.** لا نعرض درجة مخترعة، ولا صفرا
كأنه حكم.

---

## 5) Content sources

| النوع | المصدر | الحالة |
|---|---|---|
| الأحاديث | `src/data/hadith.ts` | **١٣٠ نصا**، راوٍ ومصدر لكل واحد |
| الأذكار | `src/data/adhkar.ts` | كما هي |
| الأدعية | `src/data/duas.ts` | كما هي |
| المصحف | AlQuran.cloud + نسخة محلية | كما هي |
| اقتباسات المستطيل | **من قاعدة الأحاديث نفسها** | كل شريحة تحمل `id` حديثها |

**اقتباس المستطيل ليس نصا مؤلَّفا.** اختباران: أن نص كل شريحة جزء من حديث قائم، وأن مصدره
يبدأ بنسبته ويحتوي مصدر ذلك الحديث.

**يحتاج مراجعة بشرية — وهي مهمة علماء لا مهمة كود:**
إثبات رقم كل نص في مصدره · إثبات درجته · مراجعة الـ١٥ أثرا والـ٤ المنسوبة · مراجعة الراوي
لكل واحد. الدالة `needsScholarlyReview` تُرجع `true` لكل النصوص: ١٣٠ من ١٣٠. يوم تبدأ مراجعة
حقيقية، ينزل هذا الرقم إلى ما روجعت منه فعلا — والاختبار يحرس النزول.

---

## 6) Notification architecture

**ليس نظاما موازيا.** `prioritizeNotifications` ظلّ مرتّب الأولويات. والجديد طبقة صياغة تسبقه:

```text
NOTIFICATION_TEMPLATES (١٢ قالبا)
   ↓ resolveApplicableTemplates  ← منطق: هل القالب ينطبق الآن؟
   ↓ selectNotifications        ← نافذة التوقيت + التهدئة
   ↓ prioritizeNotifications    ← الترتيب (المحرّك القائم)
   ↓ appendNotification         ← السجل: سقف ٥٠ + منع التكرار
```

**ضد الإغراق، ثلاث طبقات مستقلة:** الشرط يقرر أن يظهر · الأولوية تقرر متى · التهدئة تقرر لا يتكرر.

**الوقار:** اختبار يرفض عبارات اللوم والضغط في كل قالب، ويرفض نصا أطول من ١١٠ حرفا.

**تسلسل الصلاة بلا إزعاج:** تذكير قبل الوقت (`prayer-lead`) · نغمة عند الوقت (`prayer-time`) ·
ولا شيء بعده، لأن `prayer-time` بمهلة ٢٤٠ دقيقة لا يتكرر في اليوم نفسه.

---

## 7) Audio architecture

**لا موسيقى ولا مؤثرات ولا ملفات صوتية.** كل نغمة مولّدة من Web Audio ومدة أقصر من ثانية.

| النغمة | القناة | المدة |
|---|---|---|
| `tap` · `toggle-on/off` | رد الفعل الخفيف | ٩٠–١٥٠ms |
| `complete` | الإنجاز | ٦٢٠ms |
| `reminder` | التنبيه | ٧٠٠ms |
| `prayer` | الصلاة (مستقلة) | ١٥٠٠ms |

**البوابة نقية** `cueAllowed` لا تلمس المتصفح، فكل قاعدة منع قابلة للاختبار آليا.
**الافتراضي صامت.** **لا تشغيل تلقائي:** يُفتح سياق الصوت عند أول `pointerdown` فقط.

**OFF يعني OFF فعلا:** المفتاح العام أولا، ثم القناة، ثم الحجم. والضغط على زر النغمة حين
الملف صامت لا يجاوز التعطيل.

---

## 8) جولة التقوية الختامية — أخطر ما فيها أنها وجد عيوبا حقيقية

لم تكن هذه جولة تلميع. **أربعة عيوب فعلية** في شيفرة Phase 3 نفسها، كل واحد يُغلق بسلوك
يظهر للمستخدم:

### H1 · تهدئة الإشعارات كانت مجمّدة طوال الجلسة
**الأثر:** المؤقّت يقرأ `items` من نسخة أول رسم، و Effect لا يعاد إلا بتغيّر `enabled`.
فتبقى تهدئة كل قالب على حالتها عند التركيب: يختار المحرّك قالبا نُبّه عليه فعلا، فيسقطه
`appendNotification`، ولا يبقى مكان لغيره — **والإشعار يضيع بتلك الدقيقة بصمت**.
**السبب الجذري:** قراءة حالة حيّة من إغلاق قديم داخل `setInterval`.
**الإصلاح:** مرآة `itemsRef` تُحدَّث مع كل رسم كما صُنع سلفا لـ`snapshot` و`audio`.
**الاختبار بعد الإصلاح:** اختبار يقرأ المصدر ويمنع العودة إلى `items`، وآخر يثبت أن
منع التكرار في الحارس نفسه لا في ترتيب المحرّك.

### H2 · تسجيل الخروج كان يُلغى بنفسه
**الأثر:** `clearLocalData()` تمسح `oud:notifications:v1`، لكن مؤقّت الإشعارات يبقى يعمل
حتى يفكّ تركيب الشاشة. فإن ضرب المؤقّت في تلك اللحظة، أعاد كتابة سجل الإشعارات **بعد المسح**،
وبقي على الجهاز أثر من مستخدم خرج فعلا.
**السبب الجذري:** المتصفح لا يُبلغ أحدا بأن أحدا كتب، فلم يكن هناك ما يمنع الكتابة.
**الإصلاح:** **عهد الجهاز** — كل مسح يرفع عدّادا، ومن يحمل عهدا أقدم يتوقف عن الكتابة.
الاعتماد في الذاكرة وحدها، فهو يغطي نافذة الإغلاق لا أكثر.
**الاختبار بعد الإصلاح:** ثلاثة اختبارات — المسح يربك العهد، والمسح المرار لا يُبقي عهدا
صالحا لكاتب قديم، والمؤقّت يلتزم بالعهد.

### H3 · صوت الإشعار كان يتبع مفتاح لم يطلبه المستخدم
**الأثر:** كانت الفئات موزّعة على قنوات صوتية أخرى: «المهمة» على قناة الإنجاز، و«العودة بلطف»
و«حديث اليوم» على قناة اللمس. **فكان إطفاء «رد الفعل الخفيف» — وهو لمسة ضغط لا تنبيه —
يُسكت إشعارا لم يُطلب إسكاته.** والعكس: إشعار يتبع مفتاح لا يعرفه المستخدم.
**الإصلاح:** قاعدة واحدة: ما دام الإشعار إشعارا، فصوته يتبع مفتاح التنبيه. الصلاة وحدها
لها نغمة مستقلة. اختفى التنويع الصوتي الصغير بين فئات التنبيه، وهو ثمن لا مكان فيه.
**الاختبار بعد الإصلاح:** اختبار يمرّ على كل الفئات ويقبل قناتين اثنتين فقط.

### H4 · استئناف الصوت كان يرفض بلا التقاط
**الأثر:** `void context.resume()` وعد يرفضه المتصفح عند رفض سياسة الاستئناف — **رفض
وعد غير معالَج** يظهر في الطرفية وقد يبتلع ما حوله. ومنشئ `AudioContext` نفسه كان يرمي
في بعض البيئات بلا التقاط.
**الإصلاح:** `resumeSafely` تلتقط الرفض، والمنشئ في `try/catch`. الصوت رفاهية، ورميه
كان يسقط التطبيق.

### إضافات التقوية
- **المستطيل كان معرَّفا في ١٤ قسما منها ١١ بلا مستهلك.** حُذف الزائد، وصار النوع مغلوبا:
  قسم رابع جديد يتركب فقط بتعديل الملف، فيظهر ذلك في المراجعة. واختبار سابق كان يجوب
  خمسة أقسام — صار يجوب الثلاثة الصحيحة.
- **جسم المؤقّت كله داخل `try/catch`:** لقطة ناقصة أو تخزين ممتلئ لا يخرجان منه.
- **خطأ كان في بوابة الجودة:** `needsScholarlyReview` مستورد في اختبار ولا مستعمل.
  بدل حذفه، استُعمل في اختبار جديد: «لا نص في القاعدة يُعتمد عليه قبل مراجعة عالم».

---

## 9) `schemaValidation: false` — أين هو ولماذا، وهل يمكن تفعيله

**أين:** `src/convex/schema.ts` السطر ٢٤٥، في الخيار الثاني من `defineSchema`.

**ما الذي فحصه هذا الفحص** (`bun run check:schema`) — وهو أُعيد بناؤه، كان سابقا يعطي أصفارا كاذبة:

| السؤال | الجواب |
|---|---|
| كم جدولا وحقلا؟ | ١٠ جداول تطبيق · ٩٣ حقلًا · **٣٣ حقلًا اختياريًا** |
| هل فهرس على حقل اختياري؟ | **لا** — لا واحد في جداول التطبيق |
| هل يعلن المخطط اعتماده على سجلات قديمة؟ | **نعم** — خمس إعلانات صريحة،منها «اختيارية: ملفات قديمة تعمل بلا كسر» و«optional for backward compatibility» |

**السبب الجذري لبقائه معطّلا:** التطبيق مصمَّم ليستقبل سجلات ناقصة الحقول، ومكتوب في المخطط
نصا. **تفعيل التحقق لا يمنع كتابة سجل ناقص، بل يجعل قراءته خطأ.** فالتفعيل يحوّل ٣٣ حقلًا
اختياريًا كانت backwards-compatible إلى ٣٣ سبب فشل قراءة.

**لماذا لم نجرّب التفعيل:** لأن أثره **غير قابل للتراجع** على النشر القائم، والبيئة هنا لا
تملك وصولا إلى بيانات النشر ولا أداة متصفح لفحص أثره. المحاولة كانت ستنتج «فشل» أو «نجاح
وهمي» ولا تميز بينهما.

**ما الذي يعوّضه الآن:** الـvalidators اليدوية عند كل حدّ في `planner.ts` و`weeklyPlans.ts`،
وفحص الفحص محفوظ كبوابة `check:schema` فأي فهرس على اختياري يظهر لاحقا.

**الشرط المحدد للتفعيل:** هجرة بيانات تعبر بكل السجلات القائمة، أو نشر جديد من الصفر.
عندها، وليس قبلها.

---

## 10) Date / week boundary

**هذا البند كان مؤجّلا في تقرير المرحلة الأولى. كُشف causa ولم يكن خللًا بل قرارًا موثّقًا:**

`weekStartOfDateKey` يحسب بـUTC عمدا، و`dateKey()` يحسب محليا.
**لأن حساب يوم الأسبوع من تاريخ معبَّر عنه في صورة تاريخ لا يتأثر بمنطقة الجهاز، يعطي
الطرفين نفس النتيجة.** فليس بينهما تعارض، ولا مواضع يلتفتان فيها.

**متى يلتفتان فعلا؟** لا يلتفطان: كلاهما يشتق من نفس تقويم التاريخ. الاختبار يمرّ على
ست مناطق زمنية (UTC · الرياض · طوكيو · نيويورك · أوكلاند · كولكاتا) وست لحظات حدّية
(قبل منتصف الليل · بعده بالثانية · رأس السنة · آخرها) ويقارن حسابين. النتيجة: صفر اختلاف.

**اختبارات `tests/date-boundary.test.ts` (١٧):** بداية الأسبوع · نهاية الأسبوع · منتصف الليل ·
حدّ المنطقة الزمنية · تغيير اليوم المحلي · الرجوع من أسبوع آخر · مفتاح أسبوع غير صالح.

---

## 11) UX changes

- **حالات أزرار موحّدة:** `default` · `hover` · `pressed` · `active` · `selected` · `disabled` · `loading` · `success`.
  النشطة تحمل حلقة مزدوجة وخطا داخليا، **فلا تعتمد على اللون وحده**.
- **بيت الأحاديث:** حديث اليوم ← حديث هذا الوقت ← المكتبة.
- **قارئ الحديث:** نص حريري بسطر ٢٫٤، المصدر في منطقة منفصلة، شريط تقدّم يظهر عند الطول فقط.
- **مركز الإشعارات:** اليوم · قبل ذلك · حالة قراءة · إدارة. لا شبكة اجتماعية.
- **قسم الصوت:** المفتاح العام + أربع قنوات + مستوى، وزر «جرّب».

---

## 12) Accessibility

| البند | الحالة |
|---|---|
| `aria-live` | حالة القراءة معلنة، والشريحة نفسها ليست |
| حالة النشطة غير اللونية | حلقة + خط داخلي + علامة صح |
| ٤٤×٤ | `touch-target` على كل شريحة وزر |
| الرسومات | `aria-hidden` + `focusable="false"`، بلا `<title>` يُقرأ |
| لوحة المفاتيح | الأسهم تنقل الشرائح، `tablist` صحيح |
| RTL | صفر أداة اتجاه فيزيائية في المكوّنات الجديدة (اختبار) |
| reduced motion | يلغي الانتقال والدوران وحلقة التحميل (اختبار) |

---

## 13) Performance

**قاعدة: قياس قبل أي تعديل.**

| المقياس | خطّ الأساس | بعد التقوية | القرار |
|---|---|---|---|
| `index` | 352.64 kB | **352.64 kB** | لم يمسّه شيء |
| `Dashboard` | 148.67 kB | **148.94 kB** | ‎+٠٫٢٧ kB للتقوية، وهي مقيسة |
| قاعدة الأحاديث | `hadith` ٥٦٫٥٤ kB منفصل | **٥٦٫٥٤ kB** | لا تزال خارج المسار الحرج |

**على الصدق في المصدر:** عمود خطّ الأساس من قياس المرحلة الأولى، وعمود «بعد التقوية» من
بناء هذه الجولة. والرقمان لا يقارنان بيُني من شجرة نظيفة، فالفرق بينهما ‎+٠٫٢٧ kB فقط
وهو رقم البناء وحده.

**لا اعتمادية جديدة.** لا مكتبة رسم متحركة جديدة، ولا Framer Motion في المكوّنات الجديدة (اختبار).

---

## 14) Tests

```text
bun test                    278 pass / 0 fail / 3363 expect() calls / 24 files
bun tsc -b --noEmit         PASS
bun run lint                0 errors / 26 warnings
bun run build               PASS — index 352.64 kB (gzip 108.13)
bun convex dev --once       PASS — Convex functions ready
bun run check:glyphs        نظيف
bun run check:schema        الفحص يعمل — لا فهرس على اختياري
```

**١٧ اختبارا جديدا** في جولة التقوية (٢٦١ ← ٢٧٨): ١٥ في ملف جديد، وواحد في اختبار الأحاديث،
وواحد في اختبار الإشعارات. وأرقام المرحلة الأولى (٢٤٠) سبقت ملف حدود الأسبوع.

| الملف | العدد | ما يحرسه |
|---|---|---|
| `phase3-hardening.test.ts` | ١٥ | التهدئة الحيّة · مسار الصوت · حدود المستطيل · عهد الجهاز · أمانة النسبة |
| `hadith-metadata.test.ts` | ٢٦ | المصدر، التكرار، الدرجة المخترعة، حالة التوثيق، المواضيع، مصدر الاقتباس، **انتظار المراجعة** |
| `notification-hooks.test.ts` | ٣٩ | الأولوية، التهدئة، منع التكرار، خلوّ النص من اللوم، نافذة التوقيت، السجل، **قناة الصوت** |
| `date-boundary.test.ts` | ١٧ | حدود الأسبوع والتاريخ عبر مناطق زمنية |
| `audio.test.ts` | ١٥ | البوابة، التعطيل، حدود المدة، الربط بالتفضيلات |
| `phase3-ux.test.ts` | ١٧ | RTL، ميزانية الحركة، reduced motion، حالات الأزرار، وصولية الرسومات |

**قبل هذه الجولة كان في خطأ lint واحد:** استيراد بلا استعمال في اختبار. أغلقته باختبار حقيقي
لا بالحذف.

---

## 15) Browser QA

**صفر اختبار بصري نُفِّذ. لا أداة متصفح في البيئة.**

**الدليل:** لا `playwright` ولا `puppeteer` ولا `selenium` ولا `webdriver` في `node_modules`
(الاسم الوحيد المطابق هو `electron-to-chromium` وهي قاعدة بيانات، لا أداة)، ولا
`chromium` ولا `chrome` ولا `firefox` على `PATH`، ولا ذاكرة `~/.cache/ms-playwright`.

الثلاثون اختبارا كلها `NOT VERIFIED` — انظر `PHASE_3_BROWSER_QA.md`.

لا ادّعاء عن: RTL بصري · أي مقاس من الستة · safe area · تركيز لوحة المفاتيح ·
التثبيت على Android · سلوك الصوت الفعلي · توقيت الإشعارات على أرض الواقع.

**ملاحظة على الصدق:** العيوب الأربعة في القسم ٨ اكتُشفت بالمراجعة والقياس، لا بالمتصفح.
**من المحتمل أن تكون هناك عيوب لم تظهر إلا على جهاز حقيقي.** هذا حدّ ما يقوله هذا التقرير.

---

## 16) Known risks

1. **قاعدة الأحاديث تحتاج مراجعة علمية.** ١٥ أثرا و٤ منسوبة، وكلها بلا رقم ولا درجة.
   صار ذلك **معلَنا** لا مخفيا، لكنه نقص في المحتوى لا في التنفيذ.
2. **صفر حديث موثّق.** لا رقم ولا مراجعة، فلا ادّعاء.
3. **الصوت لم يُختبر في متصفح.** البوابة نقية ومختبَرة، لكن Web Audio الفعلي مجرّب آليا.
4. **حارس عهد الجهاز في الذاكرة.** يغطي نافذة الإغلاق، لا كتابة بعد تحديث صفحة.
   ثم إن كانت المراجعة في هذا الملف كافية: بعد التحديث لا كاتب حيّ في الذاكرة.
5. **`schemaValidation: false`** — موثّق في القسم ٩ مع شرط التفعيل.
6. **`Dashboard` أكبر بـ٠٫٢٧ kB** عن Phase 3 الأولى. ثمن مقيس.
7. **٣٠ اختبارا بصريا لم تُنفَّذ.** هذا أخطر بند في القائمة.

---

## 17) Remaining work

- [ ] ٣٠ اختبار Browser QA على جهاز حقيقي
- [ ] مراجعة ١٣٠ نصا بعلماء وإثبات الأرقام والدرجات
- [ ] هجرة بيانات ثم تفعيل `schemaValidation`
- [ ] قياس أداء فعلي على جهاز

---

## 18) Release readiness

| المستوى | الحالة |
|---|---|
| **IMPLEMENTED** | ✅ |
| **AUTOMATION VERIFIED** | ✅ ٢٧٨ اختبارا · tsc · lint · build · convex · glyphs · schema |
| **HARDENED** | ✅ أربعة عيوب حقيقية أُصلحت واختُبرت |
| **BROWSER VERIFIED** | ❌ **NOT VERIFIED** |
| **RELEASE READY** | ❌ **لا** |

> ### `PHASE 3 — IMPLEMENTED · HARDENED · AUTOMATION VERIFIED · BROWSER QA NOT VERIFIED`
>
> **ليست جاهزة للإطلاق.** أربعة مستويات لا تختلط:
> `implemented` ليس `verified`، و`automation verified` ليس `browser verified`.
> ولا مرحلة تُغلق قبل بوابة المتصفح الحقيقية.
