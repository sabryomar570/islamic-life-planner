# AUDIT_PHASE_0 — تدقيق ما قبل التنفيذ

> **تاريخ التدقيق:** 2026-09-24. **قاعدة الأدلة:** كل ادعاء في هذا الملف إما (أ) مأخوذ من قراءة الكود سطرًا سطرًا، أو (ب) نتيجة أمر تشغيل فعلي (`bun test`, `bun run build`, `bunx convex dev --once`, `bunx tsc -b --noEmit`, `bun run lint`, فحص الشبكة). **ما لم يُختبر مكتوب حرفيًا أنه لم يُختبر.**
>
> **حدود منهجية معلنة:** هذه البيئة لا تتيح متصفحًا آليًا ولا جهازًا حقيقيًا؛ لذلك **لم يُشغَّل التطبيق كتجربة مستخدم كاملة**. ما جرى التحقق منه فعليًا: سلامة البناء، الاختبارات، الكودجين، الفحص النوعي، جرد الملفات، تحليل التدفق من الكود، والتحقق من حالة المستودع العام على GitHub. أي حكم UX أدناه مبني على قراءة التدفق والكود، وموسوم بوضوح.

---

## 1) الحالة الحالية للمشروع

- **المنتج**: تطبيق ويب إسلامي عربي (RTL) بواجهة PWA، اسم العرض الحالي متذبذب بين «عود» (الأيقونة والمانيفست) و«Islamic Life Planner» (عنوان الصفحة/اسم المستودع).
- **التقنية**: React 19 + TypeScript + Vite 7 + Tailwind v4 + shadcn/ui + Framer Motion، الخادم Convex (استعلامات reactive + Convex Auth)، التغليف والاختبارات بـ Bun.
- **الحالة الفنية المؤكدة بالتشغيل:** `bun test` ⇒ **48/48 ناجح**؛ `bunx tsc -b --noEmit` ⇒ **صفر أخطاء**؛ `bunx convex dev --once` ⇒ **كودجين ونشر ناجح**؛ `bun run build` ⇒ **ناجح** ومخرج `dist/` بحجم **1.4MB**.
- **البنية**: 11 قسمًا داخليًا (اليوم، صلاتي، المصحف، الأدعية، الأحاديث، المسبحة، الأبيات، قصص الأنبياء، المناسبات، محفوظاتي، الإعدادات) + Landing + Auth + Onboarding + Dashboard محمي، مع Service Worker ومانيفست.
- **المستودع**: `github.com/sabryomar570/islamic-life-planner` **عام**، متزامن تلقائيًا (Vly/Freebuff) — تحققت من سجل الـcommits فعليًا خلال هذا التدقيق.
- **البيئة**: Freebuff Web — تدير dev server وConvex؛ لذلك المطلوب التحرير عبر أدوات الملفات فقط، والتحقق عبر `convex dev --once` + `tsc` + `test` + `build`.

## 2) ما الموجود فعليًا (جرد)

| الطبقة | الموجود | الملفات المرجعية |
|---|---|---|
| المصادقة | Email OTP + Anonymous (Convex Auth)، جلسة دائمة (localStorage + كعكة 400 يوم)، تجاوز أوفلاين عند انقطاع الشبكة | `convex/auth.ts`, `auth/emailOtp.ts`, `lib/auth-storage.ts`, `components/RequireAuth.tsx`, `hooks/use-auth.ts` |
| البيانات | `profiles`, `prayerLogs`, `adhkarLogs`, `favorites` + فهارس صحيحة، كل mutation محمي بـ `requireUserId` وقوائم قيم مغلقة | `convex/schema.ts`, `convex/planner.ts` |
| المواقيت | Aladhan + اختيار طريقة الحساب + احتياط ثلاثي (ذاكرة 6س → آخر محفوظ → جداول مدمجة) | `hooks/use-prayer-times.ts`, `lib/prayers.ts` |
| الموقع | استنتاج تلقائي من المنطقة الزمنية (بلا إذن) + إحداثيات اختيارية بإذن + عكس ترميز جغرافي عبر Nominatim | `lib/location.ts`, `hooks/use-location.ts` |
| الأذكار | 5 مجموعات (صباح/مساء/نوم/بعد الصلاة/الهمّ) بمصادرها وتكرارها + تسجيل إنجاز يومي | `data/adhkar.ts`, `components/app/AdhkarDialog.tsx` |
| القرآن | قراءة مستمرة آية‑بآية، تمرير تلقائي بـ5 سرعات، علامة موضع + آخر موضع، تنزيل كامل بمفتاح واحد، سلسلة تخزين: ذاكرة → IndexedDB → مدمج (سور قصيرة) → شبكة | `lib/quran-store.ts`, `components/app/QuranView.tsx` |
| المحتوى | أحاديث مبوّبة برواة ومصادر، أدعية، أبيات، قصص أنبياء، مناسبات هجرية بعدّادات | `data/*` |
| المحفوظات | استعلام reactive + مرآة محلية + تفاؤل فوري + حذف صريح آمن (`removeFavorite` لا يُدرج أبدًا) | `hooks/use-favorites.ts`, `planner.removeFavorite` |
| التذكيرات | جدول يومي (صلاة/قبل الصلاة/الأذكار/الورد/النوم/الجمعة/رمضان) بنافذة إطلاق 10د وإزالة تكرار + رسالة «هل صلّيت؟» بعد الصلاة | `hooks/use-reminders.ts`, `lib/notify.ts` |
| التنبيهات اللطيفة | محرك First-party (بلا إزعاج): نافذة واحدة كل مرة، أولويات، احترام حالة إذن الإشعارات، منع التكرار اليومي | `hooks/use-nudges.ts`, `components/app/NudgeCenter.tsx` |
| PWA | SW بثلاث حافظات (shell/runtime/api)، cache-first للأصول، network-first للصفحات، صفحة احتياط بلا شبكة، اختصارات مانيفست، تحديث تلقائي | `public/sw.js`, `lib/pwa.ts` |
| التصميم | نظام «زجاجي» موحّد (glass/glass-strong/tile-edge/btn-edge) + خطوط عربية (IBM Plex Arabic + Amiri + Aref Ruqaa) + RTL كامل | `index.css`, `components/app/GlassCard.tsx` |
| المنصة | `vly-toolbar-readonly.tsx` + `@vly-ai/integrations` + `instrumentation.tsx` + `main.ts` (Hono/Deno يخدم `dist`) | ملفات المنصة (تُبقى — تبعية حقيقية) |

## 3) ما يعمل وما لا يعمل (بصدق)

**مؤكد بالتشغيل:**
- ✅ البناء الكامل، الفحص النوعي، الكودجين، 48 اختبار وحدة (وقت/هجري/مواقيت).
- ✅ كل واجهات Convex محمية بالمصادقة وبمدخلات مغلقة (قراءة كود كاملة لـ`planner.ts`).
- ✅ المستودع العام موجود ومتزامن (تحقق شبكي فعلي).

**مؤكد بقراءة الكود فقط (لم يُختبر في متصفح):**
- 🟡 التدفق الكامل: Landing → /auth → /dashboard → Onboarding → Dashboard → الإعدادات.
- 🟡 الإشعارات والتذكيرات ومحرك التنبيهات (منطق مقروء، إطلاق حقيقي غير مختبَر هنا).
- 🟡 الأوفلاين والمصحف (منطق السلسلة مقروء، سلوك IndexedDB غير مختبَر هنا).

**لا يعمل / ناقص فعليًا:**
- ❌ لا يوجد نظام خطة (Plan) ولا مهام ولا جدولة مفهومة للمستخدم — «نظام الحياة» في الرؤية غير موجود بعد في المنتج.
- ❌ لا توجد مراجعة يومية (Daily Review) ولا محاسبة حقيقية.
- ❌ لا يقيس التطبيق إلا الصلوات والأذكار — لا دراسة/عمل/نوم/عادات/أهداف كما تقول الرؤية.
- ❌ الـOnboarding 7 أسئلة فقط، لا فهم لحياة المستخدم (دراسة/عمل/نهاية اليوم/مستوى الحزم/تركيز الأسبوع).
- ❌ لا توجد صفحة خصوصية داخل المنتج ولا حذف كامل للبيانات من داخل التطبيق.
- ❌ لا PNG icons (SVG فقط) — iOS يتجاهل أيقونة SVG، وبعض أدوات webapk تتطلب 192/512 PNG.
- ❌ `bun run lint` يفشل: **23 خطأ + 33 تحذيرًا** (تفصيلها في §21).

## 4) ما يجب الحفاظ عليه (لا يُلمس خيره)

1. **نظام التصميم الزجاجي** وأدواته (`glass`, `tile-edge`, `btn-edge`, `rail`, `quran-text`) — هوية بصرية متماسكة بالفعل.
2. **طبقة الخادم في `planner.ts`**: النمط (requireUserId + قوائم مغلقة + assertDate/assertTime + cleanText) ممتاز ويُبنى عليه لا يُستبدل.
3. **`removeFavorite` المنفصل عن `toggleFavorite`** — إصلاح أمني/وظيفي صحيح أُنجز سابقًا ويُحفظ.
4. **اختبارات bun للطبقة الخالصة** (48) + بنية `lib/` القابلة للاختبار — توسيعها لا استبدالها.
5. **سلسلة تخزين المصحف** وPWA (SW بحافظات منفصلة) — يعملان بتصميم سليم.
6. **مؤقّتات `use-clock` المشتركة** (bucketed) — نمط أداء جيد يجب أن يبقى.
7. **النسخ الاحتياطي في `getStats.compute` عبر استعلام نطاق واحد** — نمط صحيح بعد الإصلاح السابق.
8. **محمية الأفكار**: تجاوز الأوفلاين للجلسة، وتصميم الاحتياط في المواقيت.

## 5) ما يجب إصلاحه (مرتبًا)

| # | المشكلة | الخطورة | الدليل |
|---|---|---|---|
| 1 | **مفتاح منصة مضمّن في `src/convex/auth/emailOtp.ts` ومؤكد في المستودع العام** | 🔴 حرجة | عدّ آلي للقيمة في raw GitHub = 1 |
| 2 | غياب «نظام الحياة»: لا خطة، لا مراجعة يومية، لا محاسبة | 🔴 حرجة للمنتج | قراءة `Dashboard/HomeView` — البنية كلها بطاقات معلومات |
| 3 | الهوية غير موحدة: `index.html` = "Islamic Life Planner"، المانيفست/الواجهة = «عود»، مفاتيح التخزين = `sakinah:*` و`oud:*` | 🟠 عالية | `index.html`, `manifest.webmanifest`, `grep` |
| 4 | Onboarding لا يفهم حياة المستخدم (7 أسئلة فقط، لا شيء عن اليوم/الحزم/التركيز) | 🟠 عالية | `data/questions.ts`, `pages/Onboarding.tsx` |
| 5 | الرئيسية غير واعية بالوقت: نفس الترتيب صباحًا وليلًا، وتتجاهل خطة اليوم | 🟠 عالية | `components/app/HomeView.tsx` |
| 6 | `bun run lint` يفشل (23 خطأ) — 21 منها قواعد React Compiler الجديدة على أنماط مقصودة، و2 حقيقية | 🟡 متوسطة | `bun run lint` |
| 7 | لا حذف كامل للبيانات من داخل المنتج (خصوصية) | 🟠 عالية | لا يوجد أي mutation حذف شامل |
| 8 | زر تسجيل الصلاة بلا حارس ازدواج ضغط (double-submit) | 🟡 متوسطة | `PrayerView` + نافذة «هل صلّيت؟» — لا disabled أثناء الإرسال |
| 9 | نص مانيفست قديم: «القرآن الكريم بصفحات المصحف» (الميزة حُذفت) | 🟡 متوسطة | `manifest.webmanifest` shortcut |
| 10 | `HomeView` يعيد التصيير كل ثانية (useNow(1000) في الجذر) رغم أن العدّاد له مؤقّته الخاص | 🟡 متوسطة | `HomeView.tsx` سطر `const now = useNow(1000)` |
| 11 | لا CSP ولا رؤية أخطاء إنتاجية (لا Sentry/بديل) | 🟡 متوسطة | لا يوجد رأس CSP ولا مراقبة |
| 12 | ملفات ميتة وضخمة: `oud-project-complete.zip` (86MB) بقايا في الجذر، `LogoDropdown.tsx` غير مستخدم، `src/assets/logo.svg` تابع له | 🟢 منخفضة | `ls` + `grep` |
| 13 | `UNVISITED_VIEWS` في محرك التنبيهات يفتقد «قصص الأنبياء» و«محفوظاتي» | 🟢 منخفضة | `use-nudges.ts` |

## 6) ما يجب حذفه

- `oud-project-complete.zip` (86MB) و`oud-project-full.zip` (386KB): نسخ تسليم قديمة، تُنشأ آليًا عبر `bun run package:zip` في `/releases` (المسار الصحيح). حذفها من القرص.
- `src/components/LogoDropdown.tsx` + `src/assets/logo.svg`: مكوّن قالب غير مستخدم بنصوص إنجليزية ولا استدعاء له (`grep` فعلي: NONE).
- تحديث أهمية: `src/components/ui/index.ts` (فهرس أدوات shadcn) غير مستخدم لكنه جزء من عُرف المكتبة — يُبقى.
- **لا يُحذف**: أي ملف من `src/convex/auth/*` أو `vly-integration/instrumentation` — تبعيات منصة حقيقية.

## 7) ما يتعارض مع الرؤية الجديدة

1. **التمركز الحالي كتطبيق محتوى** (Landing يقول: صلاة + أذكار + مصحف + أحاديث) — لا يقول أبدًا «يعرف حياتك، ينظمها، يحاسبك، يتعلم منك».
2. **غياب النتيجة الزمنية**: لا شيء في المنتج يخبر المستخدم «ماذا أفعل اليوم؟» — بينما هذا جوهر الرؤية.
3. **المحاسبة**: التطبيق يسجّل ويعرض، لكنه لا يسأل «لماذا لم تفعل؟» ولا يقترح تصحيح المسار.
4. **الحزم الاختياري**: شخصية المنتج الجديدة (مدرّب منضبط) تحتاج مستوى تدخّل يختاره المستخدم — غير موجود.
5. **الاسم والهوية**: «عود» كأيقونة جيد، لكن عنوان الصفحة وسرد المنتج غير موحدين؛ لا شعار نصي ولا وعد مكتوب.
6. **Dashboard كجدار بطاقات**: 9 بطاقات في الشريط + 4 بطاقات + مناسبتان — مخالف لمبدأ «لا تعرض 30 بطاقة».
7. **الترتيب الثابت**: لا فرق بين فتح التطبيق 7:30 صباحًا وبعد العصر.

## 8) مشاكل UX (من الكود، مع التصريح بعدم وجود اختبار متصفح)

1. **قبل القيمة**: CTA الأساسي من Landing يذهب إلى `/auth` مباشرة — المستخدم يُسأل عن حساب قبل أن يرى قيمة ملموسة (يوجد مسار ضيف Anonymous لكنه غير بارز في الـCTA).
2. **Dashboard مزدحم**: 11 قسمًا + شريط اقتباسات + 9 بطاقات في الرئيسية؛ لا تسلسل «الأهم الآن».
3. **الـOnboarding استبيان** لا محادثة: 7 أسئلة بلا رابط «كيف ستستخدم إجاباتي؟» عميق.
4. **لا حلقة يومية**: لا شيء يغلق اليوم (مراجعة) أو يفتح الأسبوع (تركيز/هدف).
5. **مصطلحات داخلية**: نصوص جيدة لكنها وصفية («الصلاة القادمة»، «ثباتك») — لا صوت مدرب.
6. **حالات فارغة**: جيدة عمومًا (بطاقة ثبات مخفية حتى وجود سجل)؛ استثناء: Banks لا يوجد EmptyState مخصص للمراجعة (غير موجودة أصلًا).
7. **تعدد مسارات الأذكار**: شريط + شبكة أذكار + MiniCard «ذكر اليوم» + MiniCard «أذكار اليوم» = تكرار بصري مفهوم لكنه مزدحم في الرئيسية.
8. **حدود ثقة**: الشاشة لا توضح للمستخدم أن تسجيل الصلاة **لا يُستخدم لتقييم ديني**، بل لبناء التزام — نقطة tone حرجة مع شخصية «محاسبة» قادمة.

## 9) مشاكل الـArchitecture

1. `planner.ts` مونوليث (7 استعلامات + 5 طفرات في ملف واحد) — مقبول الآن، يُقسّم عند نموه (plan/tasks/review tables قادمة).
2. `schemaValidation: false` — نفق حماية إضافي معطّل؛ يجب التحول التدريجي إلى validators لكل جدول قبل توسعات كبيرة.
3. ازدواج مصادر الحالة المحلية (localStorage mirrors: favorites/prefs/offline day state) — يعمل لكنه يحتاج قلبًا موثقًا وعقدًا واحدًا (`lib/offline-store.ts` جزئي فقط).
4. لا طبقة i18n: النصوص العربية سطرية في المكوّنات — إضافة الإنجليزية لاحقًا ستحتاج تمريرًا منهجيًا (لا تعارض حاليًا).
5. لا مفاتيح إصدارات/Feature flags — تغيير سلوك onboarding والواجهة يجب أن يتحمل بيانات قديمة (تظهر معالجة بـ optional fields).
6. اتجاه الاستيراد: `lib/time.ts` يستورد من `lib/hijri.ts` (اتجاه واحد، لا دورة) — مقبول.

## 10) مشاكل الأداء

1. **إعادة تصيير الرئيسية كل ثانية** (HomeView: `useNow(1000)`) — الأثقل على الأجهزة المتوسطة؛ الإصلاح: 30 ثانية في الجذر + العدّاد يستقل بـ1 ثانية.
2. `getStats` يعيد بناء سجل 30 يومًا بكل نداء (مقبول كحجم، لكنه استعلامان بحد 1000 سجل لكل مستخدم نشط).
3. ميزانية الحزم جيدة: `dist` 1.4MB؛ المصحف المدمج 8KB (سور قصيرة) — لا مشكلة bundle الآن.
4. لا قياس أداء حقيقي (RUM) في الإنتاج — القياس غير موجود لا الأداء.
5. `getHistory` أُصلح لنمط النطاق (استعلام/جدول) — ✅.

## 11) مشاكل Accessibility

1. أزرار الأيقونات في الهيدر لها `aria-label` جيد ✅؛ لكن أدوات QuranView وبعض أزرار البطاقات الصغيرة تحتاج تدقيق تسميات (لم تُختبر بقارئ شاشة).
2. `prefers-reduced-motion` مُحترم في CSS (`float-soft`) لكن ليس في كل حركات Framer Motion (QuoteMarquee).
3. تباين: النصوص `text-muted-foreground` بدرجة 0.55 على خلفية شبه بيضاء ≈ جيد حديًا؛ النصوص 10px في بطاقات صغيرة أصغر من الحد المريح (يُقترح 11px كحد أدنى).
4. ترتيب العناوين: h1 لكل قسم داخل صفحة واحدة بعد التنقل الفوري — SPA بلا مشكلة SEO (المشروع تطبيق محمي، والـLanding وحدها عامة).
5. لا `lang` ديناميكي (كل الواجهة عربية — كافٍ الآن).

## 12) مشاكل Mobile/PWA

1. **أيقونات SVG فقط**: iOS يتجاهل apple-touch-icon SVG (أيقونة مفقودة عند التثبيت)، وأدوات بناء APK (Bubblewrap) تطلب PNG 192/512. — لا يمكن توليد PNG في هذه البيئة (لا ImageMagick/Canvas)؛ توثيق + توصية.
2. لا حشوة لأعلى الآمن (safe-area-inset-top) في الهيدر الملتصق — على iPhone بالتثبيت قد يلتصق المحتوى بالنوتش.
3. نصوص المانيفست القصيرة تحتاج تحديثًا (shortcut مصحف قديم العبارة).
4. `viewport-fit=cover` موجود ✅، الشريط السفلي يحترم `safe-area-inset-bottom` ✅.
5. الرسائل عبر SW (`NOTIFY`, `CACHE_URLS`) سليمة؛ نسخة `sw.js` الحالية `oud-2026-09-d` تحتاج رفعًا بعد تعديلات الدفعة القادمة.
6. لا اختبار PWA فعلي (Lighthouse) في هذه البيئة — مذكور كحد.

## 13) مشاكل الهوية والعلامة

- **ثلاث هويات متجاورة**: عنوان الصفحة «Islamic Life Planner» • المانيفست والأيقونة «عود» • مفاتيح التخزين `sakinah:*` (اسم أقدم) و`oud:*`.
- شعار دهني حرفي («عود» بنص أبيض على تدرّج سماوي‑نيلي) — لطيف لكنه عام ولا يحمل دلالة (العود = الآلة/الرجوع؟ التوتر دلالي: العود في العربية «الرجوع»، والآلة الموسيقية).
- لا شعار نصي (tagline) مكتوبًا في المنتج.
- نبرة الكتابة جيدة لكنها **موصِفة وليست موجِّهة** — تحتاج تحويلًا من «هذه ميزة» إلى «هذا ما سيحدث لك».
- أسماء مقترحة (للنقاش في BRAND_DIRECTION.md) قابلة للبناء عليها دون حرق الهوية الحالية.

## 14) مخاطر أمنية

| المخاطرة | الشدة | الدليل |
|---|---|---|
| مفتاح منصة مضمّن في الكود ومؤكد في المستودع العام | 🔴 حرجة | فحص raw GitHub عدّ = 1 |
| لا CSP (يتضخم أثر أي XSS محتمل مع مرآة جلسة طويلة في localStorage) | 🟠 عالية | `index.html` بلا رؤوس/سياسة |
| لا rate limiting ظاهر على mutations (يعتمد الحدود الافتراضية لـConvex) | 🟡 متوسطة | قراءة `planner.ts` |
| بيانات المصحف/الموقع تذهب لطرف ثالث (Aladhan/alquran.cloud/Nominatim) بدون ذكره صراحة داخل التطبيق | 🟡 متوسطة | `use-prayer-times.ts`, `use-location.ts` |
| لا صفحة خصوصية داخل المنتج ولا حذف بيانات | 🟠 عالية | لا يوجد |
| لا مراقبة أخطاء إنتاجية | 🟢 منخفضة | لا يوجد Sentry |

**إيجابيات يجب الإبقاء عليها**: كل الطفرات محمية + قيم مغلقة + تنظيف نصوص + محدّد معرّف عناصر + حدود أقصى للمحفوظات/التاريخ — التزام أمني جيد على مستوى الخادم.

## 15) الأسرار والمفاتيح المكشوفة — **إفصاح بلا قيم**

- `src/convex/auth/emailOtp.ts` (ملف قالب منصة) يحتوي مفتاح إرسال OTP بادئته معروفة، **وقد تم التحقق آليًا (عدّ فقط) أنه موجود الآن في المستودع العام** ⇒ يلزم **تدوير (Rotation) للمفتاح من جهة المنصة** + إزالته من الشجرة الحالية (سيُنفَّذ الآن عبر env) + تنظيف تاريخ Git (توصية للمنصة/المالك).
- لا أسرار أخرى وجدتها في الشجرة النشطة (`.env*` محجوبة بالـ`.gitignore`، وسكربت التغليف يعمّي المفتاح داخل حزم `/releases`).
- لم تُطبع أي قيمة سرية في هذا التقرير ولا في أي تقرير آخر.

## 16) ملفات مترهّلة/ضخمة

| الملف | الحجم | القرار |
|---|---|---|
| `oud-project-complete.zip` | 86MB | 🗑️ حذف (بقايا تسليم قديمة، تُعاد بالـscript عند الحاجة) |
| `oud-project-full.zip` | 386KB | 🗑️ حذف (نفس السبب) |
| `LogoDropdown.tsx` + `assets/logo.svg` | ~4KB | 🗑️ حذف (غير مستخدمين) |
| `dist/` | 1.4MB | يُبقى (مُستثنى من المستودع) |
| `node_modules` | — | مُستثنى أصلًا |
| `src/components/ui/*` (~50 ملفًا) | ~300KB | يُبقى — مكتبة shadcn جزء من كيت التصميم |
| `public/sw.js` + أيقونات SVG | صغير | يُبقى |

## 17) الاعتماد على Freebuff (حدود حقيقية)

1. `vly-toolbar-readonly.tsx` (الجذر) + `@vly-ai/integrations` + `src/instrumentation.tsx` + `main.ts` (سيرفر Deno) + `vite.config.ts (vlyPlugin)` — **تبعيات منصة حيّة**، لا تُحذف؛ ربط الواجهة الرمادية (toolbar) يظهر في وضع المعاينة فقط.
2. `auth.freebuff.app/send_otp` — مرحّل بريد OTP للمنصة؛ مفتاحه المضمّن هو المخاطرة أعلاه. خطة التخلي المستقبلية: تمرير مفتاح عبر env (يُنفَّذ الآن) ثم استبداله بمزوّد بريد خاص عند الإنتاج المستقل.
3. مستودع GitHub العام + التزامن الآلي + نشر Convex dev: تديرها المنصة — لا يمكن تشغيل `git` من هذا الوكيل (مؤكد: الأوامر محجوبة). **الالتزامات تظهر تلقائيًا** (تحقّق فعلي من سجل commits). هذا يعني: التسليم للمستودع يتم بالمنصة، لا بأمر مني.
4. لا يمكن تشغيل متصفح/APK builder هنا — أي ادعاء عكس ذلك سيكون كذبًا.

---

## خلاصة القرار للمرحلة القادمة (Phase 0.6)

بناءً على كل ما سبق: **لا إعادة بناء**. الدفعة التنفيذية الأولى تستهدف بالترتيب:
1. إزالة السر من الشجرة (env) + توثيق التدوير (لا نُصلح التاريخ من هنا، نمنع التكرار ونبلغ).
2. تحويل الـOnboarding من استبيان إلى **فهم حياة** (+4 أسئلة مؤثرة فعليًا، لا حشو).
3. **مراجعة يومية** حقيقية مربوطة بمؤشر أسبوعي عادل (تُنفَّذ خادميًا + واجهيًا).
4. **رئيسية واعية بالسياق** (صباح/نهار/مساء/ليل) بترتيب أولويات متغيّر + سطر خطة اليوم.
5. حذف كامل للبيانات + تصدير أوسع (خصوصية كوعد لا كصفحة تسويق).
6. إصلاحات صلبة صغيرة: حارس ازدواج تسجيل الصلاة، رفع `useNow` الجذري إلى 30s، نصوص المانيفست، lint الحقيقي، تنظيف الملفات الميتة.

**ما لن يُنفَّذ الآن (وسببه):** خطة/مهام أسبوعية كاملة (تحتاج تصميم UX + جداول جديدة — المرحلة التالية)، محرك توصيات، Vision للتمارين، الإنجليزية، premium — كلها موثقة في UX_PHASE_0.md كمسار لاحق، لأن تنفيذها بلا اختبار متصفح في هذه البيئة مخاطرة عبثية.
