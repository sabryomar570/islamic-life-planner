# PRODUCT_EVOLUTION_PLAN — ما يجب أن يصبح عليه «عود»

## Vision
أهدأ وأسرع رفيق يومي للمسلم على الويب: يفتح في ثانية، يعمل بلا إنترنت بالكامل، لا يعرض إعلانًا ولا يرسل موقعك، ويغلق حلقة اليوم بخاتمة أسبوعية مرئية. **«ليس كل شيء — لكن كل يوم»**.

## Target Users
1. **الأساسي**: مسلم عربي 18–45 يسعى لثبات الصلوات والأذكار، يكره الإعلانات في تطبيقات العبادة، وغالبًا على هاتف متوسط.
2. **الثانوي**: مستخدم إفريقي/آسيوي بشبكة ضعيفة — الأوفلاين الكامل يخدمه حرفيًا.
3. **غير مستهدَف الآن**: باحث أكاديمي، مستخدم لا عربي له، متسوّق مجتمعي (Ummah) — لا نبني لهم حتى يتغيّر القرار.

## Core Value Proposition
> «كل شيء ديني يحتاجه يومك، بلا إعلان واحد، بلا اشتراك، وبلا أن يغادر موقعك جهازك — ويظل يعمل والطائرة لا تهبط.»

الدليل التنافسي: Muslim Pro يجعل الأوفلاين مدفوعًا وإعلاناته دافع هجرة موثّق (مراجعة 04/2025)، وAthan Pro يشارك الموقع لأطراف ثالثة (بلوك Play). هذا هو الثغرة التي نقف فيها.

## Differentiation (الذي سيجعلنا مختلفين فعليًا)
1. **أوفلاين كامل مجاني** — بما فيه المصحف كاملًا، دون «Premium».
2. **خصوصية مصارح بها** — نص واحد واضح في الهبوط والإعدادات: موقعك لخدمة المواقيت فقط.
3. **حلقة مركّزة** — صلاة → ذكر → ورد → سجل → **خاتمة أسبوعية** (البند الجديد).
4. **خفّة** — PWA بعدة ميغا ميقاتية لا 324MB.

## Product Architecture (النسخة الجديدة)
لا إعادة بناء — نعم تدعيم:
- **Frontend**: كما هو (React+Vite) مع إضافة بطاقة «أسبوعك» في HomeView المصدر الوحيد لها getStats.
- **Backend**: Convex كما هو مع mutation حذف صريح + استعلام نطاق موحّد.
- **المحتوى**: بيانات محلية (src/data) كما هي — مصدر قوة.
- **PWA/SW**: كما هي مع رفع VERSION عند أي تغيير أصول مستقبلًا.

## UX Architecture (الرحلة المثالية)
فتح → (جولة مرة واحدة) → 7 أسئلة بلا تسجيل إلزامي → Home تعرض «اليوم» + «أسبوعك» → نقرة تسجيل صلاة → نافذة أذكار في وقتها → تنبيه «هل صلّيت؟» بنافذة مهلة → الختام: شريط تقدم أسبوعي غير متطفّ. حالات الفشل محسوبة بالفعل (أوفلاين: جلسة مخزنة + بيانات محلية + دمج عند العودة).

حالات كل شاشة موجودة أصلاً (فارغ/تحميل/خطأ عبر toast)؛ الإضافة الجديدة «أسبوعك» تُخفى كليًا حتى تتوفر بيانات (لا فارغ محرج).

## Feature Roadmap (مرتب)
1. **P0 (الآن)**: إصلاح remove الغاشّ، النصان الكاذبان، getHistory نطاقيًا، getStats بالتوقيت المحلي.
2. **P1**: بطاقة «أسبوعك» على الرئيسية (تقرأ getStats الحالي) + سطر الخصوصية في الهبوط.
3. **P2**: اختبارات Bun للوقت/الهجري + لمسات وصول (aria محسّنة على أيقونات الحفظ).
4. **P3 (لاحق)**: وضع موسم رمضان على الرئيسية، تقرير أسبوعي إشعاري، ترقيم المحفوظات.

## Technical Roadmap
- **فورًا**: `removeFavorite` في planner.ts؛ `getHistory` بـ gte واحد/جدول؛ `getStats` يبني التواريخ من `dateKey()` المحلي؛ نصّا Landing/nudge.
- **بعد ذلك**: بدون مكتبات جديدة — بطاقة الأسبوع من getStats الموجود.
- **لا تفعل**: ترقيم صفحات الآن، درجات أمان env الآن (emailOtp ملف منصوي — توثيق فقط)، أي مكتبة UI جديدة.
