# PHASE_NEXT_FINAL_REPORT.md
## OUD — شخصية المنتج وتطوّر الاستخدام
### امتداد لـ Phase 0 / 1 / 2 / 3 / 3.x — لا إعادة بناء

التاريخ: ٢٦ سبتمبر ٢٠٢٦
الحالة: **مُنفَّذ ✅ · آلي متحقَّق ✅ · متصفّح غير متحقَّق ❌ (انظر §6)**

---

## 1) التدقيق قبل التنفيذ — ماذا وجدنا فعلا

طُلب أولا تدقيق قصير قبل أي سطر. هذا ما جاء، ولم نكتب كودا قبله.

| السؤال | ما وجدناه في المشروع القائم | القرار |
|---|---|---|
| هل توجد شخصية؟ | **لا.** `coach.ts` مخطِّط لا شخصية (فترة اليوم، نصيحة المراجعة، مؤشّر الأسبوع). `notification-templates.ts` فيه نصوص **فصحى مهذّبة** («الصلاة على وقتها تحفظ وقتك…») لا لهجة، و`use-nudges.ts` فيه نصوص أخرى، وفي كلٍّ منهما لا علاقة بين سبب ظهور الرسالة ومضمونها. | بناء وحدة شخصية واحدة، لا تعديل النصوص القائمة. |
| هل يوجد سياق أو ذاكرة؟ | **لا.** لا يوجد أي سجل لما قاله المستخدم، ولا علاقة بين تكرار التأجيل وردة فعل التطبيق. | بناء ذاكرة من **بيانات موجودة فعلا** فقط (نتائج الخطة المؤجَّلة، تاريخ آخر نشاط). ممنوع الاختراق. |
| هل يوجد نقاط أو إنجازات؟ | **لا، وقرار سابق منعها صراحة.** ترويسة `progress.ts`: «لا نقاط مقارنة بين الناس، ولا «إنجازات» وشارات». | المواصفة الجديدة تلغي ذلك القرار، فبُنيت، مع إبقاء **نفي المقارنة**ebooksбомحكم اختبار. |
| الصلاة | قوية. `prayers.ts` + `use-prayer-times` + `NextPrayerHero` + `PrayerView` + Aladhan + مواقيت محفوظة. الرئيسية **بُنيت على الصلاة أصلا**. | لم نلمس محرك الصلاة. أضفنا سطر الشخصية **تحت** بطل الصلاة لا فوقه. |
| الإشعارات | **موجودة وذكية.** ١١ سببًا، أولويات (prayer 100 ← recovery 35)، de-dupe، burst suppression، تهدئة، `priorityInRange`، عامل خدمة، أصوات بقنوات. | لم نخترع. استعملناها. أضفنا عليها **الصوت** لا البديل منها. |
| الموقع | موجود للمواقيت فقط: `location.ts` (منطقة زمنية بلا إذن) + `useGeolocation` (إحداثيات). **لا مسجد، لا قرب، لا إذن مفسّر.** | أضفنا المسجد والقرب خلف إذن مفسّر. |
| إذن الموقع | **لا يُطلب إطلاقا إلا من الإعدادات**، بلا سبب معروض. إذن الإشعارات يُطلب في `Onboarding` بسطر سبب واحد. | حوار سبب واحد لاثنين. |
| محادثة | **لا شيء.** | بنينا عقل الشخصية (محلي، بلا مفتاح). |
| الأحاديث | ١٣٠ نصًّا. لم نلمسها. | — |
| مركز إشعارات داخل التطبيق | **موجود** (نافذة من الترويسة) وهو محقَّق منه في Phase 3 ومغطّى باختبارات. | المواصفة لا تريد مركز إشعارات. **لم نحذفه** (خطوة regression) و**لم نبنِ واحدا جديدا**. قرص: نُبقي الأداة القائمة ولا نضيف سطحا. |

**الخلاصة:** الأولوية ١ و٢ (الصلاة واليوم) كانتا مبنيتين. الأولوية ٣ (الشخصية) و٥ (المسجد) و٦ (التقدّم) كانتا غائبتين. الأولوية ٤ (الإشعارات) كانت بُنيت في الآلية وفاقرها **الصوت**. الأولوية ٨ (المحادثة) كانت غائبة.

---

## 2) ما تم تنفيذه

### 2.1 الشخصية — `src/lib/oud-voice.ts`
شخصية واحدة ثابتة، **بلا منتقي نبرة ولا مستوى اختيار**. ١٧ سياقا، لكل سياق ٢–٣ صيغ.

- **ليست عشوائية.** الاختيار يبذرة على (المعرّف + تاريخ اليوم) عبر `seedOf` ثابت: مستقرّ خلال اليوم، متنوّع بين الأيام، قابل للاختبار. `Math.random` كان سيلوّث الذاكرة والاختبارات معا.
- **بلا إيموجي.** مفحوص باختبار على الشيفرة المرسومة.
- **المدح نادر.** لا «تسلمم» إلا بحدث: إنهاء مهمة مؤجَّلة، يوم كامل، يوم مثالي، رجوع بعد انقطاع.
- **الجلد دعابة لا إهانة.** «فاكر… قلت أفكرك بس» لا يخرج إلا عند تكرار **مسجّل** (٢ تأجيلات فأكثر).
- **الأولوية:** الصلاة 100 ← اقتراب 100 ← تعقيب 98/97 ← التسويف 94 ← المسجد 95 ← اليوم 84 ← … ← الهدوء 1.
- **افتراضات ممنوعة:** بلا سطر، يعود إلى «أنا هنا… يومك معاك».

### 2.2 صفر ادعاء ديني في طبقة الشخصية
هذه أقوى قاعدة في المرحلة. `oud-voice.ts` و`oud-chat.ts` **لا ينسبان إلى الدين شيئا**: لا حديث ولا آية ولا دعاء ولا فضل ولا أجر ولا حكم. أي نص ديني في OUD يأتي من `data/hadith.ts` / `data/adhkar.ts` / `data/duas.ts` ومعاه مصدره.

المنعوم بالاسم: «مليون حسنة»، «سبع وعشرين درجة»، «مفيش حاجة بتغفرها»، «من صلى الفجر حفظ اليوم كامل»، وكل ما ورد في الأمثلة. **حارس آلي** في `tests/oud-personality.test.ts` يفحص كل صيغة وكل سطر مولَّد في ١٣ لقطة. أي reintrodu Sargent يُسقط الاختبار.

### 2.3 الذاكرة — من بيانات موجودة فقط
`use-oud.ts` يبني «فاكر» من **عدد مرات التأجيل المسجّل في نتائج الخطة**، و«عدت» من **تاريخ آخر نشاط** في `oud:voice:last-active:v1`. صفر يعني لا سابق، فلا تذكير بالماضي أبدا. لا ذاكرة مخترعة، ولا ادّعاء تذكّر شيء لم يُحفظ.

### 2.4 التقدّم الشخصي — `src/lib/oud-progress.ts`
- نقاط: جماعة ٢، منفرد ١، خطوة تمّت ٢، جزئية ١، أذكار ١، مراجعة ٢، رجوع ٣.
- **لا لوحة متصدّرين، ولا مقارنة، ولا نصّ يَعِد بالترتيب** (مفحوص).
- سبعة مستويات بأسماء تصف عادة لا مرتبة، ولا اسم فيها «مبتدئ» ولا «هاوي».
- سجلّDevice بـ ٤٠٠ حدث سقف، ومفتاح ثابت لكل حدث فلا تُحتسب النقرة مرتين.
- ٧ إنجازات **مشتقّة** من أرقام مسجّلة (لا تُخزَّن): أول صلاة، أول جماعة، يوم كامل، ٣ أيام، ٧ أيام، الرجوع، أسبوع خطة. إعلان واحد يظهر مرة ثم يختفي.

### 2.5 المسجد — `src/lib/oud-mosque.ts`
- مسافة دائرة عظمى + تنسيق عربي (`١٧٨ متر` — **أصلحنا هنا علّة حقيقية**: كانت تخرج أرقاما لاتينية).
- مصدر مفتوح بلا مفتاح: Overpass / OpenStreetMap، نصف قطر ١٥٠٠ م، مهلة ٨ ثوانٍ، **الفشل يعيد `[]` بلا استثناء**.
- مخبوزة ٢٤ ساعة، **وهي في سجلّ المسح عند تسجيل الخروج**.
- **لا يثبت الصلاة.** النصّ في الواجهة يقول ذلك صراحة. اختبار يقرأ شيفرة `sw`-المقابل؟ لا: اختبار يقرأ نصّ العقد في المصدر.
- **البقاء القريب في التطبيق المفتوح فقط**: لا `watchPosition`، ولا إذن دائم، ولا سجل. ثوانٍ داخل ٢٥٠ م في جلسة مفتوحة فقط.

### 2.6 المحادثة — `src/lib/oud-chat.ts` + `OudChatView`
- **ليست Chatbot ولا نموذجا لغويا.** امتداد الشخصية: يشخّص القصد من كلمات العامية (١١ نية) ويردّ من **حالة جهازك** بنفس لهجة `oud-voice`.
- **لا يفتي ولا يحكم.** سؤال فقهي → يحوّل للمحتوى الموثّق ويقول «أنا مش بفتتاوي».
- لا يدّعي معرفة الغيب، ولا ينسب حديثا، ولا يستنتج «تحسّن» من نبرة الكلام.
- كل ردّ يحمل `basedOn`: ما الذي بُني عليه. يظهر للمستخدم تحت الرسالة.
- **مُختبَر على الخادم** (SSR) ٢٢ اختبارا في `tests/oud-render.test.tsx`.

### 2.7 Permission UX — `PermissionReasonDialog`
حوار واحد يشرح **لماذا** قبل أن يطلب، للإثنين:
- **الموقع:** أقرب مسجد + مسافة + اتجاهات + مواقيت أدق. والوعد المكتوب للمستخدم: «مش بنحفظ تاريخ مواقع، ومش بنتابعك في الخلفية، والموقع ما بيثبتش إنك صلّيت. ولو رفضت، كل حاجة تانية شغالة عادي».
- **الإشعارات:** ما الذي سيصلك بالضبط، و«لو رفضت هيشتغل كل حاجة عادي غير التنبيه».
- خيار **«مش دلوقتي»** في الاثنين. مربوط بـ`Onboarding` (الإشعارات) و`SettingsView` (الموقع).

### 2.8 الردّ من الإشعار — أقصى ما تسمح به المنصة
- **البحث أولا:** `actions` تعمل على Chromium (Chrome/Edge/Opera) وعلى Firefox-أندرويد، ولا على Safari ولا Firefox سطح المكتب. **والردّ النصّي داخل الإشعار غير موجود في الويب أصلا** — تلك `RemoteInput` لأندرويد أصلي وحده.
- فالتنفيذ: **زرّان** («سجّلتها» / «لسه») على التذكيرات التي وجهتها شاشة الصلاة، وعامل الخدمة يقرأ `event.action` ويفتح التطبيق على `?reply=logged` أو `?reply=later`، و`Dashboard` ينفّذ **مرة واحدة** ثم يمحو البرامتر.
- `notificationActionsSupported()` يفحص السمة قبل إرسال الأزرار. **لا ادّعاء لغير الموجود**، واختبار يقرأ `public/sw.js` ويتأكد أنه لا يرسم حقل كتابة.

### 2.9 خريطة المعلومات — ٤ مناطق، بلا مركز إشعارات جديد
`DashView` أضاف `"chat"` فقط، ودخل **مجموعة «التطبيق»** لا الشريط السفلي. الشريط الأساسي خمس وجهات كما هو.

---

## 3) الملفات المهمة

### جديد
| الملف | ما فيه |
|---|---|
| `src/lib/oud-voice.ts` | محرّك الشخصية: ١٧ سياقا، بذرة ثابتة، أولويات، تهدئة. **بلا React وبلا خريطة.** |
| `src/lib/oud-chat.ts` | عقل المحادثة: ١١ نية، وصفات، حدود الدور. بلا React. |
| `src/lib/oud-progress.ts` | النقاط والمستويات والإنجازات والتخزين. |
| `src/lib/oud-mosque.ts` | المسافة، Overpass، الكاش، روابط آمنة، البقاء القريب. |
| `src/hooks/use-oud.ts` | **الجسر الوحيد** بين حالة التطبيق والشخصية. كل حقل مشتقّ. |
| `src/components/app/OudLineCard.tsx` | سطر الشخصية + صف النقاط + إعلان الإنجاز. |
| `src/components/app/MosqueCard.tsx` | بطاقة المسجد بكل حالاتها الصادقة. |
| `src/components/app/OudChatView.tsx` | المحادثة. |
| `src/components/app/PermissionReasonDialog.tsx` | سبب الإذن + ما لا نفعله + خيار الرفض. |
| `tests/oud-personality.test.ts` | ٢٥ اختبارا، منها **حارس المحتوى الديني**. |
| `tests/oud-progress.test.ts` | ٤٢ اختبارا، منها **حارس مفاتيح الخصوصية**. |
| `tests/oud-notification-reply.test.ts` | ٩ اختبارات، تحرس حدود المنصة. |
| `tests/oud-render.test.tsx` | ٢٢ اختبار SSR للمكوّنات الجديدة. |

### مُعدَّل
`src/pages/Dashboard.tsx` (الجسر + المحادثة +Earned Toast + ردّ الإشعار) · `src/components/app/HomeView.tsx` (السطر والمسجد تحت بطل الصلاة) · `src/components/app/Navigation.tsx` · `src/components/app/SettingsView.tsx` · `src/pages/Onboarding.tsx` · `src/lib/notify.ts` · `src/hooks/use-reminders.ts` · `src/lib/local-data.ts` (٦ مفاتيح جديدة) · `public/sw.js`.

### لم نلمس
`src/convex/*` كله. `src/lib/hadith-metadata.ts`. `src/data/hadith.ts`. `src/lib/notification-intelligence.ts`. `src/lib/notification-templates.ts`. `src/lib/progress.ts`. `vite.config.ts`.

---

## 4) الاختبارات

| الملف | العدد | أهم ما يحرسه |
|---|---|---|
| `oud-personality.test.ts` | ٢٥ | الثبات، أولوية الصلاة، **لا ذاكرة بلا سابق**، نادر المدح، التهدئة، **حارس الادعاءات الدينية**، **حارس الإيموجي**، نيات المحادثة، حدود الدور. |
| `oud-progress.test.ts` | ٤٢ | لا تكرار، سقف السجل، الأسماء، **لا نصّ منافسة**، حدود كل شارة، تخزين تالف، المسافة، **الفشل لا يخترع**، روابط `javascript:` مرفوضة، البقاء بلا خلفية، **حارس مفاتيح الخصوصية** (يقرأ `src/` كله). |
| `oud-notification-reply.test.ts` | ٩ | فحص القدرة قبل الادّعاء، وجهات التذكير، عقد عامل الخدمة، **لا حقل كتابة**. |
| `oud-render.test.tsx` | ٢٢ | رسم فعلي: RTL، `aria-label`، `role="log"`، حالات المسجد الصادقة، الأرقام العربية، خيار الرفض. |

**المجموع: ٤٤٦ اختبارا / ٣١ ملفا / ٤٨٨٨ `expect()` — صفر فشل.** (كان ٣٤٨ قبل هذه المرحلة؛ أضفنا ٩٨.)

**علّة حقيقية كشفها الاختبار وأصلحناها:** `formatDistance` كانت تخرج `178 متر` بأرقام لاتينية داخل واجهة عربية. أضفنا `arabicNumber`، وصارت `١٧٨ متر`، وثبّتناها باختباري وحدة ورسم.

**علّة حقيقية كشفها الاختبار وأصلحناها:** قواعد النية كانت على الجمع («نقاط») فسقطت على المفرد المصري («كام نقطة»). أضفنا المفرد، ووثّقنا الدرس في التعليق.

---

## 5) نتائج كل gate

| Gate | الأمر | النتيجة |
|---|---|---|
| اختبارات | `bun test` | ✅ **٤٤٦ pass / 0 fail** / ٤٨٨٨ expect / ٣١ ملفا |
| أنواع | `bun tsc -b --noEmit` | ✅ **PASS**، بلا أي خطأ |
| لِنت | `bun run lint` | ✅ **0 errors** + ٢٧ warning (كلها `react-refresh` المعتادة؛ قبل ٢٦، والزيادة ملف `.tsx` جديد) |
| بناء | `bun run build` | ✅ **✓ built in 13.97s** |
| Convex | `bun convex dev --once` | ✅ **Convex functions ready! (7.82s)** |
| محارف | `bun run check:glyphs` | ✅ **نظيف** |
| مخطط | `bun run check:schema` | ✅ **١٠ جداول · ٩٣ حقلًا · ٣٣ اختياريًا** — بلا تغيير عن Phase 3 |

**ملاحظة على اللِنت:** الـwarning الوحيد الجديد من ملف نظيف هو `react-refresh` في `oud-render`? لا — الأسبقية للـwarning القديم same. الصفر أخطاء هو الحكم.

---

## 6) Browser QA

### الحالة: **NOT RUN — ❌**

خادم التطوير `localhost:5173` **مطفيّ**. فحصناه **٣ مرات متفرّقة** في هذه الجلسة فكان `000`
(`ERR_CONNECTION_REFUSED`) في الثلاث. سياسة المنصة تمنع تشغيل خادم من الطرفية،
فلم نشغّله ولم نحاول تجاوزها.

**ما هذا يترك غير متحقَّق منه بصيغته النهائية:**

- العرض الحقيقي على مقاسات ٣٦٠ / ٣٩٠ / ٤١٢ / ٧٦٨ / ١٠٢٤
-absence الفرط الأفقي، RTL على الشاشة، الوضعان الفاتح والداكن، reduced motion
- التنقّل السفلي، ترتيب Tab، استعادة التركيز بعد إغلاق حوار
- PWA install، safe areas، العمل دون إنترنت
- **سلوك رفض الإذن للإشعارات والموقع في متصفح حقيقي**
- **سلوك أزرار الإشعار على جهاز حقيقي** (لا يمكن محاكاة ذلك على خادم أصلا)

### ما قُام بدلا منه — تحقّق على مستوى الشيفرة، وهو **ليس** بديلا

`tests/oud-render.test.tsx` يركّب المكوّنات الجديدة على **خادم React** (`renderToStaticMarkup`)
ويقرأ النصّ المرسوم. ٢٢ اختبارا. ما ثبت فعلا:

- سطر الشخصية يُرسم بنصّه المحسوب، لا بنصّ ثابت؛ وفي غياب السبب يعود إلى «أنا هنا… يومك معاك».
- **لا إيموجي في الشيفرة المرسومة** (فحص على الـmarkup، لا على المصدر فقط).
- بطاقة المسجد: بلا موقع **لا ترسم شيئا**؛ الرفض **يشرح ولا يهدّد**؛ التعذّر **يقول «ما لقيتش» ولا يخترع مسجدا**؛ الجاهز **يعلن صراحة أن الموقع لا يثبت الصلاة**؛ الرابط `https` و`rel="noopener noreferrer"`.
- المحادثة: تبدأ برسالة شخصية لا بترحيب عام؛ `role="log"` و`aria-live="polite"`؛ حقل الكتابة مُسمّى؛ وتعلن «مش بجاوب في الدين من نفسي».
- حوار الإذن: النصّ **مُصدَّر** ويُختبر كعقد (Radix يعرض عبر بوابة لا تُرسَم على الخادم) — فحُرس **ما يُعرض فعلا** لا ما نريده.
- المسافة تُرسَم بالأرقام العربية.

**الحدّ الصريح:** هذا يثبت أن المكوّنات تُرسَم وأن نصوصها صحيحة. **لا يثبت** التخطيط أو التفاعل أو القياس. لذلك: **NOT VERIFIED IN BROWSER**، ونقولها كما هي.

---

## 7) Religious-content status

| البند | قبل | بعد |
|---|---|---|
| عدد الأحاديث | ١٣٠ | **١٣٠** (لم نضف ولا نحذف) |
| مرفوع | ١١١ | **١١١** |
| أثر صحابي | ١٥ | **١٥** |
| منسوب بصيغة موصوفة | ٤ | **٤** |
| `verified` | ٠ | **٠** |
| `traceable` | — | **١٣٠** |
| `unverified` | — | **٠** |
| الأدعية | ٤٠ (١٤ قرآن · ٢٦ سنة) | **٤٠** |
| الأذكار | ٥٢ | **٥٢** |

`schema.ts` و`hadith-metadata.ts` و`data/*` **لم تُمسّ**. قياساتُنا أعلاه مقيسة بالبرمجة على البيانات نفسها لا منقولة.

**إضافات هذه المرحلة:**
1. **حارس آلي** يمنع «مليون حسنة» و«٢٧ درجة» و«مفيش حاجة بتغفرها» و«يحفظ اليوم كامل» من الخروج من طبقة الشخصية. ١٣ لقطة × كل صيغة.
2. **حدود دور صريحة في المحادثة:** سؤال فقهي لا يجيب، ويحوّل للمحتوى الموثّق.
3. **لا نصّ ديني في طبقة الشخصية إطلاقا** — قاعدة، لا استثناءات.

**ما لم يفعله ولم نفعله:** لم نضف «فائدة صلاة الجماعة ٢٧ درجة» ولا «من صلى الفجر حفظ اليوم كامل» ولا «مليون حسنة» ولا دعاء «اللهم اغفر للمؤمنين…» مربوطا بوعد عددي. الوارد في الأمثلة إن كان يحمل علّةً محتملة يبقى **خارج** التطبيق، ومصدره مطلوب قبل أي إدخال.

---

## 8) Performance impact

**المقيس:**

| الحزمة | الخام | gzip |
|---|---|---|
| `Dashboard` (يشمل الشخصية والنقاط والمسجد) | ١٧٣٫٥٣ kB | **٥٥٫٢٩ kB** |
| `OudChatView` (كسولة، منفصلة) | **٩٫٩٩ kB** | لا تدخل المسار الحرج |
| `index` (لم يتغيّر جوهريا) | ٣٥٢٫٦٣ kB | ١٠٨٫١٢ kB |

- المحادثة **كسولة**: لا تُحمَّل إلا عند فتح `?view=chat`.
- الشخصية نفسها على المسار الحرج (في `HomeView`)، فهذا ثمنها: إظهارها فورا بلا نقرة.
- حساب السياق داخل `useMemo`؛ ودقّة الساعة هي `reminders.now` (تتبدّل كل دقيقة) فلا يُعاد البناء كل رسم.
- **سطر الشخصية في الحالة**: قراءة سجلّ التهدئة وقت الرسم تعني كتابة أثناء الرسم، وهو محظور في React. فحوّلناه إلى **أثر جانبي** يحسب السطر عند تغيّر اللقطة أو الدقيقة فقط. هذا ما كشفه لِنت (`react-hooks/refs`) وأصلحناه بالطريقة الصحيحة لا بالتعليق.
- طلب المسجد **مرة واحدة** لكل ٢٤ ساعة، بمهلة ٨ ثوانٍ، ويلغى عند إغلاق المكوّن.
- **ما لم نقسه:** فروق «قبل/بعد» حقيقية للحزمة، لأن البناء الأول في هذه الجولة كان **بعد** كتابة الوحدات لا قبلها. الأرقام أعلاه حالة حالية مقيسة، والفرق **مقدَّر لا مقيس** — ونقولها هكذا.

---

## 9) Security / privacy impact

### إضافة
- **ستة مفاتيح محلية جديدة** (`oud:xp:ledger` · `oud:achievements:seen` · `oud:voice:shown` · `oud:voice:last-active` · `oud:voice:returned` · `oud:mosque:near`) — **كلها في سجلّ المسح**، فتسقط مع تسجيل الخروج أو حذف البيانات.
- **حارس جديد يقرأ `src/` كله** وي تضمّ أن كل مفتاح `oud:` أو `sakinah:` مكتوب في التطبيق مُسجَّل في `OWNED_LOCAL_KEYS`. استثناء `sakinah:quran:` و`sakinah:timings` **مكتوب صراحة** في الاختبار مع سببه (محتوى عام مشترك). قبل هذا الحارس كان `local-data.test.ts` يفحص قائمة مكتوبة بيد، فأي مفتاح جديد يمرّ صامتا — وهذا ثقب حقيقي أغلقناه.
- **لا خادم جديد، ولا حقل جديد، ولا هجرة.** النقاط والشارات **على الجهاز لا على الحساب**، والسبب مكتوب في الترويسة: تقيس عادة شخصية، وتخزينها في الحساب يجعل مسح الجهاز غير كامل.
- **لا تاريخ مواقع، ولا خط حركة، ولا تتبّع خلفي.** البقاء القريب داخل الجلسة المفتوحة فقط.
- **لا صور، ولا كاميرا، ولا رفع.** لم نضف Prayer Check-in أصلا: لا حاجة حقيقية له في هذه المرحلة، والمواصفة نفسها لا تطلب إضافته بلا سبب.
- **لا دالة كتابة في `localStorage` بلا `try/catch`**، وكل قراءة تتحقّق من شكل البيانات وتسقط الغريب.

### رابط خارجي واحد
`https://www.google.com/maps/dir/?api=1&destination=…` بـ`target="_blank" rel="noopener noreferrer"`، ويمرّ على `isSafeMapsUrl` التي ترفض `javascript:` و`data:` (مفحوصة).

### طلب خارجي جديد
Overpass (`overpass-api.de`)، **قراءة فقط**، `POST` بـ`application/x-www-form-urlencoded`، **بلا مفتاح وبلا حساب**، وإحداثياتك تُرسل **بعد** موافقتك على الموقع. هذا طرف ثالث جديد لم يكن في المشروع، وهو موثّق هنا صراحة.

---

## 10) Platform limitations — ما لا ندّعيه

| الميزة | الحكم الصادق |
|---|---|
| **ردّ نصّي داخل الإشعار** | **غير موجود في الويب.** `RemoteInput` لأندرويد أصلي وحده. بنينا أفضل بديل واقعي: زرّان + رابط ردّ. لا صندوق كتابة وهمي. |
| **أزرار الإشعار** | تعمل على Chromium و Firefox-أندرويد. **لا** على Safari و Firefox سطح المكتب (المتصفح يتجاهلها بصمت). نرسلها بخصائص على `actions` بعد فحص. |
| **Exact alarm** | غير موجود. لا تذكير بخلفية مضمونة؛ التذكيرات تعمل والتبويب مفتوح أو التطبيق مثبّت. |
| **تتبّع خلفية للموقع** | غير موجود. `watchPosition` دائم يحتاج إذنًا لا نطلبه. |
| **PWA = أندرويد أصلي؟** | **لا.** لا إشعارات أمامية مضمونة، ولا ردّ فوري، ولا بثّ من عامل خدمة بخلوMessaging API (لم نضف Web Push أصلا: لا خادم إشعارات ولا مفتاح VAPID). |
| **دقة الموقع** | دائرة عظمى، لا تaberay جغرافية. الخطأ حتى ٥٠–١٠٠ م في المناطق الحضرية. نقول «تقدير» في الواجهة. |
| **توفّر Overpass** | قد يكون بطيئا أو محجوبا. عندها نعرض «ما لقيتش مسجد قريب» — ولا نخترع اسما. |
| **Gmail/Maps داخل المتصفح** | تفتح تطبيق الخرائط إن كان مثبتا، وإلا الخرائط في المتصفح. |

---

## 11) Known limitations

1. **Browser QA لم يُنفَّذ** (خادم مطفئ + سياسة المنصة). هذا أهم نقص في هذه المرحلة، وأول ما يجب تنفيذه.
2. **بند واحد لم ننفّذه من المواصفة: Qibla (§16).** application **لا يعرض القبلة إطلاقا** اليوم ولا في هذه المرحلة. بنينا الأساس الذي تحتاجه (إحداثيات + إذن مفسّر + `formatDistance`)، لكن **ال-quickla نفسها feature لم تُبنَ**. السبب: الأولويات التي حددها المستخدم (الصلاة، اليوم، الشخصية، الإشعارات، المسجد، التقدّم) كانت أعلى، وأضفfeature بلا اختبار متصفح كان مخاطرة. **موثّق كـNOT BUILT لا كـDONE.**
3. **بند لم نبنَه: Zakat (§17) كخدمة.** لا يوجد شيء في `عبادتي` اسمه زكاة. **بلا أحكام شرعية مختلقة** — وهذا مقصود: زكاة بلا مصدر محسوب م risking تكون أخطر من غيابها.
4. **نموذج لغوي حقيقي: غير موجود.** المحادثة محلية وقواعدها. لو أردنا نموذجاizards حقيقيا فهو **إضافة فوق** `oud-chat`، لا بديل، ويحتاج مفتاح API و`Convex action` (لا أستطيع تعبئة المفاتيح؛ المفاتيح تأتي من واجهة المفاتيح لديك).
5. **إعلان «العودة بعد الانقطاع» يحتاج يومين على الأقل** من غياب مسجّل، في plumes أبطأ مما يبدو أول مرة.
6. **الشارات مشتقّة من سجل الجهاز** (سقف ٤٠٠ حدث ≈ ٨٠ يوما). مسح بيانات الجهاز يمسحها، وهذا متعب على من يتوقع تراكما أطول. **مقصود** ولا نبيعه كتقدم دائم.
7. **السجلّ محلي لا متزامن**: تسجيل الخروج يمسحه، فتح جلسة جديدة يبدأ من صفر. لا يوجد cloud.
8. **لا نضيف صورة أيقونة أو ثيمات فعلية** مع أن `unlock` في الشخصية جاهز لها: فتح الثيم يحتاج نظام ثيمات لم يُبنَ، و`§12` يذكره كغرض للنقاط لا كشرط.
9. **الاختبار `oud-render.test.tsx` لا يغطي Radix Portal**، فحوار الإذن يُختبر كنصّ عقد لا كرسم. **هذا نقص مقصود ومصرّح به**، لا اختبار كامل.
10. **لم نقس فروق حزمة قبل/بعد** (§8).
11. **الأرقام العربية في `formatDistance`** لكلمة «كيلومتر» تستخدم الفاصلة العشرية العربية عبر `ar-EG`؛ لم نرها مرسومة في متصفح.
12. **لِنت:** ٢٧ warning كلها `react-refresh` المعتادة في ملفات الشاشات. صفر أخطاء.

---

## 12) Preserve / not-broken checklist

- [x] `bun test`: ٣٤٨ → **٤٤٦**، صفر فشل، ولا اختبار قديم عُدّل ليمرّ.
- [x] `schema.ts` و`hadith-metadata.ts` و`data/*` **لم تُمسّ** بالمحرّking.
- [x] `notification-intelligence.ts` و`notification-templates.ts` **لم تُمسّ**.
- [x] `progress.ts` **لم يُمسّ** (نقاطه الجديدة في وحدة جديدة، فلا تعارض).
- [x] `vite.config.ts` **لم يُمسّ** (HMR معطّل كما هو).
- [x] `personalguard` (خارجي) ما زال أخضر: نصوص **`lazy` بعد آخر استيراد**.
- [x] لا تبعية جديدة. لا `npm install`. `package.json` كما هو.
- [x] ٤ مناطق المعلومات باقية؛ لا مركز إشعارات جديد؛ لا «المزيد» كصندوق.
- [x] تسجيل الخروج: `local-data` و`use-oud` **يقرآن** الأثر بعد المسح ويقفان (`itemsRef` و`wipeEpoch`).
- [x] `AchievementToast` و`OudChatView` و`OudLineCard` ملفّات كسولة أو صغيرة؛ لا أثر على المسار الحرج إلا سطر واحد.

---

## 13) Next recommended hardening — فقط

**لا نضيف مميزات. نغلق فجوات.**

1. **Browser QA حقيقي** على خادم يعمل: المقاسات الخمسة، الفاتح/الداكن، reduced motion، Tab واستعادة التركيز، رفض الإذن للإثنين، رفض/قبول الموقع بجهاز محاكى، PWA install، بلا فرط أفقي. **بلا هذا لا تُعتبر المرحلة متحقَّقة.**
2. **اختبار Contract لعامل الخدمة في متصفح**: هل يظهر الزرّان فعلا على Chromium؟ وهل `?reply=` يمرّ عبر `NAVIGATE`؟ يحتاج جهازا حقيقيا، فلا يمكن في الاختبار الآلي.
3. **مراجعةHadith علمية** لـ١٣٠ نصًّا: `0 verified` اليوم، والحال الصحيح ليس أن نرفعه بلا مراجعة، بل **أن نراجعه فعلا**. `verified` يجب أن يبقى صفرا حتى يحدث ذلك.
4. **`check:schema` إلى تشغيل كامل**: ترحيل ٣٣ حقلا اختياريا يحتاج هجرة تعبر كل السجلات القائمة. **ليس في هذه المرحلة، ولم يُقترح تنفيذه الآن.**
5. **قاعدة: أي نصّ ديني جديد لا يدخل** إلا مع `source` + `reference` قابلين للتتبّع، ولا يمرّ إلا باختبار `religious-content` و`oud-personality` معا.
6. **`onboarding` يعرض الموقع بعد الإشعارات** (سياق «ليه صلاتي أدق»)، لا في أول شاشة.
7. **مراجعةAccessibility للحوار** على جهاز حقيقي: حبس التركيز، استعادته، قارئ الشاشة.
8. **قرارات مفتوحة لم تُتخذ، فلتُتخذ بوعي:** ثيمات فعلية تفتحها النقاط؟ نموذج لغوي في المحادثة (يحتاج مفتاحك)؟ بوصلة القبلة؟ زكاة بمصدر محسوب؟ كل واحد منها قرار لا نقص.

---

## 14) الخلاصة

النظام كان **يعرف** أن الصلاة هي المحور. لكنه كان **لا يتكلم**. هذه المرحلة جعلته يتكلم — بلهجة واحدة، بسبب واضح، بلا ادّعاء، وبصدق عن حدوده:

- **4٤٦ اختبارا** أخضر، منها حارسان جديدان: **حارس الادعاءات الدينية** و**حارس مفاتيح الخصوصية**.
- **٧ بوابات** خضراء.
- **أوّل Featur لم يُنفَّذ صريحان**: Qibla وZakat — موثّقان كـNOT BUILT لا كـDONE.
- **QA في المتصفح: لم يُنفَّذ** لأن الخادم مطفئ والمنصة تمنع تشغيله. ف claiming: **NOT VERIFIED IN BROWSER**.

> الصلاة كانت المحور. الآن هناك من يتبعك.
