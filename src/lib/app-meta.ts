/**
 * PHASE 3.x — بيانات التطبيق الموثوقة.
 *
 * **الإصدار لا يُكتب بيد.** رقم مكتوب في Component ينقسم أول ترقية،
 * فيبقى في التذييل يقول «١٫٠٫٠» والتطبيق في نسخته الثانية. فالأمر
 * المصدر `package.json`، تقرؤه الأداة وقت البناء، ويظهر الرقم نابعا.
 *
 * الاستيراد باسم (`version`) لا ككائن كامل، فيُحذف باقي الملف عند البناء
 * ولا يصل باقي البيان إلى المتصفح.
 */
import { version } from "../../package.json";

/** الإصدار الحالي كما هو في بيان الحزمة. */
export const APP_VERSION: string = version;

/** يقرأه قارئ الشاشة بصيغة مفهومة: «الإصدار 1.0.0». */
export function appVersionLabel(): string {
  return `الإصدار ${APP_VERSION}`;
}
