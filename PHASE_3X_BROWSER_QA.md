# PHASE_3X_BROWSER_QA.md

**التاريخ:** 2026-09-25
**الحالة:** `PARTIALLY VERIFIED — 17 اختبارا نُفِّذت · 16 PASS · 1 FAIL غير مؤكد · الباقي لم يُنفَّذ`

> **هذه أول مرة في تاريخ المشروع يشغّل فيها متصفح حقيقي.** ليست كاملة، وهي
> **ليست** «لا شيء». ما لم يُنفَّذ مذكور باسمه، وما لم يُثبت مذكور بأنه لم يُثبت.

---

## 1) البيئة — كيف أمكن تشغيل متصفح

| البند | التفاصيل |
|---|---|
| الأداة | Playwright 1.49.1 + Chromium 1148 (headless shell) |
| **أين** | `/tmp/oud-qa` — **خارج المستودع** |
| **المستودع** | **لم يُمَسّ**: لا `package.json` ولا `bun.lock` ولا أي ملف |
| مكتبات النظام | `npx playwright install-deps chromium` (بإذن صريح) — ٢١ مكتبة كانت ناقصة |
| الخادم | `http://localhost:5173` — خادم التطوير المُدار |
| المتصفح شغّال | `TITLE: عود — رفيق يومك المسلم` |

**الدليل على أن المتصفح حقيقي لا محاكاة:** قرأ `dir="rtl"` و`lang="ar"` من DOM حيّ،
وحمل عنوان الصفحة من React الفعلي، ونقر أزرار حقيقية، وقرأ الحافظة بعد نسخ حقيقي
(`clipboard="01550324450"`).

**تسجيل الدخول:** ضيف (`الدخول كزائر سريع`) ثم onboarding كامل بأربعة خطوات.
لم يُستعمل حساب اختبار، ولم تُكتب أي بيانات اعتماد في أي ملف.

---

## 2) الاختبارات المنفَّذة فعلا — ١٧

| # | الاختبار | الأداة | النتيجة |
|---|---|---|---|
| 1 | قسم المطوّر يُعرض | قرأتُ النص المرئي | **PASS** — `OUD Founder & Developer of OUD عمر صبري` |
| 2 | RTL | `html[dir]` | **PASS** — `dir=rtl` |
| 3 | ترتيب الأقسام الستة | مواضع النصوص في النص المرئي | **PASS** — `[31,153,307,501,646,756]` تصاعديا |
| 4 | روابط الحسابات | `href` من DOM حيّ | **PASS** — `t.me/omarsaabry` · `instagram.com/_rcfo` |
| 5 | لا اسم مستخدم مرئي | النص المرئي | **PASS** — لا `omarsaabry` ولا `_rcfo` نصّا |
| 6 | لا GitHub ولا رابط TikTok | النص المرئي | **PASS** — لا `github`، لا `tiktok.com`، وبطاقة TikTok ظاهرة «قريبًا» |
| 7 | رقم المحفظة غائب عن الصفحة | النص المرئي | **PASS** — لا `015` |
| 8 | الإصدار من الحزمة | النص المرئي | **PASS** — `الإصدار 1.0.0` |
| 9 | نافذة الدعم تفتح | نقر زر «ادعم OUD» | **PASS** |
| 10 | رقم We Pay | نص النافذة | **PASS** — `015 50324450` |
| 11 | سبب التحويل | نص النافذة | **PASS** — «مخصصة لاستقبال دعم تطوير تطبيق OUD» |
| 12 | لا لغة دفع شخصية | نص النافذة | **PASS** |
| 13 | **نسخ الرقم** | نقر + قراءة الحافظة | **PASS** — `تم نسخ رقم المحفظة` + `clipboard="01550324450"` |
| 14 | Escape يغلق النافذة | `keyboard.press("Escape")` | **PASS** |
| 15 | استعادة التركيز بعد الإغلاق | `document.activeElement` | **FAIL — غير مؤكد** |
| 16 | أهداف اللمس ≥ ٤٤ بكسل | `getBoundingClientRect` على كل زر ورابط | **PASS** — لا عنصر أصغر |
| 17 | بلا تجاوز أفقي (٣٩٠) | `scrollWidth - clientWidth` | **PASS** — `overflow=0px` |

**إضافة غير مطلوبة في القائمة، وهي ثمينة:**

| # | الاختبار | النتيجة |
|---|---|---|
| 18 | **أخطاء Console** عبر الجلسة | **PASS — صفر** |
| 19 | **فشل Network** عبر الجلسة | **PASS — صفر** |

---

## 3) الـFAIL — بكل ما يعني

**«استعادة التركيز بعد إغلاق النافذة»**

- **المشكلة:** بعد `Escape`، كان التركيز على عنصر في الترويسة («عادالصلاة القادمة»)
  لا على زر «ادعم OUD» الذي فتح النافذة.
- **السبب المرجّح — وهو Artifact من الاختبار لا عيب في التطبيق:**
  الفتح جرى بـ`locator.click()` البرمجي، وRadix يستعيد التركيز إلى العنصر الذي
  كان «مركَّزًا» عند الفتح. النقر البرمجي قد لا يجعل العنصر هو `activeElement`
  الحقيقي، فيستعيد Radix شيئا آخر.
- **الاختبار الذي كان سيحسم:** فتح النافذة بلوحة المفاتيح وحدها
  (`Tab` حتى الزر ثم `Enter`)، ثم `Escape`، ثم قراءة `activeElement`.
- **الحالة:** **لم يُنفَّذ.** خادم التطوير تعطّل قبله.
- **الخلاصة الصادقة:** **لا أستطيع أن أدّعي أنه عيب في التطبيق، ولا أن أدّعي أنه
  ليس موجودًا.** هو `FAIL غير مؤكد` حتى يثبت أحدهما.

> **هذا أوّل FAIL في تاريخ المشروع يُسجَّل من متصفح حقيقي.** وكل ما قبله كان
> `NOT VERIFIED` بلا تنفيذ.

---

## 4) اختبارات لم تُنفَّذ — والسبب

**خادم التطوير `localhost:5173` تعطّل أثناء الجلسة** (`ERR_CONNECTION_REFUSED`)،
وأنا لا أُشغّل خوادم التطوير من الطرفية لأنها مُدارة.

| لم يُنفَّذ | لماذا مهم |
|---|---|
| ٣٦٠ · ٣٩٠ · ٤١٢ · ٧٦٨ · ١٠٢٤ | تجاوز أفق عند المقاسات الأخرى |
| Light / Dark | توافق الثيم |
| reduced motion | إلغاء الحركة |
| فشل الحافظة | محاكاة الرفض في متصفح حقيقي |
| تنقّل الشريط السفلي | تبديل الشاشات |
| Refresh / persistence | بقاء الحالة بعد التحديث |
| Safe areas | شاشة بحافة |
| Tab order | ترتيب التنقل بلوحة المفاتيح |
| PWA | التثبيت والعمل المستقل |

**الاختبار ١٧ غطّى ٣٩٠ فقط.** فـ«بلا تجاوز أفقي» مُثبت عند ٣٩٠، **لا عند الباقي.**

---

## 5) ملاحظتان حقيقيتان ظهرتا أثناء التنفيذ

**أ) نافذة تذكير تعترض الصفحة.** تذكير «أذكار المساء في انتظارك» يفتح فوق أي شاشة
عند فتح التطبيق، فيمنع النقر تحته. **هذا سلوك مقصود** (مذكّرات الأذكار)، وقد عالجته
في الاختبار بإطفاء التذكيرات، لا بإصلاح التطبيق. **يسجّل كسلوك لا كخلل.**

**ب) تدفّق onboarding معطّل مبدئيًا.** زر «التالي» معطّل حتى تُملأ حقتا الوقت،
وزر «لاحقًا» ينقل إلى الصفحة الرئيسية **دون حفظ الإجابات** — فيعود المستخدم
إلى onboarding. **ليس عيبا في هذه المرحلة** (سلوك سابق)، لكنه يربك أي اختبار آلي
يسير على التدفق، فوثّق.

---

## 6) كيف يُعاد التشغيل

الخادم يعود إلى `localhost:5173`، ثم:

```bash
cd /tmp/oud-qa
node session.mjs    # ضيف + onboarding + يحفظ state.json
node suite.mjs      # الاختبارات السبع عشرة
```

الحالة المحفوظة في `state.json` تجمع `localStorage` و`cookies`. **وكل ما في
`/tmp/oud-qa` خارج المستودع**، فإعادته لا تلمس المشروع.

---

## 7) الخلاصة

| المستوى | قبل | الآن |
|---|---|---|
| متصفح في البيئة | ❌ لا | ✅ نعم |
| اختبارات متصفح مُنفَّذة | ٠ | **١٩** |
| `PASS` | ٠ | **١٨** |
| `FAIL` | — | **١، غير مؤكد** |
| `NOT VERIFIED` | الكل | المتبقّي (٩ مجموعات) |

> **`PHASE 3.x — PARTIALLY BROWSER VERIFIED`**
>
> **ليست `BROWSER VERIFIED`.** المقياس صار أخضر جزئيا لا كاملا:
> ما رآه المتصفح من القسم الجديد صحيح، وما لم يُرَ بعد لم يُختبر.
> **والـFAIL الواحد معلّق على سبب لم يُحسم، والأرجح أنه في الاختبار لا في
> المنتج — لكن ذلك غير مُثبَت، ولا يُعتد به.**
